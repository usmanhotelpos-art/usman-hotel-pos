# Project Memory / Agent Notes

## Task: Customer app v1.1.0 — real SMS OTP login + per-customer orders + admin-removal + GPS location — DONE (Sep 2026)
- **User:** "aur update dai do customer app pa" — (1) kisi bhi Pakistani number par real SMS code, (2) har customer sirf apne orders, (3) customer login page se Admin/Owner login hatao, (4) top header se admin/owner icons FULL remove, (5) location functional = phone GPS via location permission, hardcoded Model Town/Gulberg areas hatao, (6) push to customer app.
- **SMS provider = Twilio** (user-confirmed). Server `uh.js` (commit `355da2c`, pushed → Railway live): `phoneE164()` (03XXXXXXXXX→+92...), `twilioConfigured()`/`sendSmsCode()` via global fetch REST `https://api.twilio.com/2010-04-01/Accounts/{sid}/Messages.json` (Basic auth, URLSearchParams form, **koi naya npm dep NAI**). Env vars needed for REAL SMS: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER` (Railway env par set karo). Jab unconfigured/unreachable → response `{success, smsSent:false, code, phone}` = in-app fallback (code UI mein dikhta hai, SMS gate se pehle dev/verify ke liye).
- **Server changes:** `POST /uh/auth/request-code` ab async — 4-digit code issue + SMS send; new user zone default `'My Location'` (fixed undefined `SessionZone()`). `POST /uh/auth/login` — **universal `1234` bypass REMOVED**, sirf exact stored (SMS-delivered) code; fail = 401 `'کوڈ غلط ہے...'`. `GET /uh/orders` — kabhi ALL nahi: Bearer token phone (override), else `?phone=`, else `[]`. `GET /uh/orders/:id` — 403 agar caller ka na ho. Naya `uhPayloadFromReq()` (non-blocking JWT parse). **Live verified:** request-code → smsSent:false fallback; correct code → 200 {token,user role CUSTOMER}; 1234 → 401; `?phone=03338877665` with token → token phone wins.
- **App changes (customer app):** `login_screen.dart` REWRITTEN — customer-only: signup(mail name)/login 2 chips, requestCode → SMS code field, fallback code box jab `smsSent:false`, verify → `loginWithCode` → `Session.saveAuth` + onLoginSuccess; `onAdminClick`/`onOwnerClick`/`selectedRoleIndex` GAYE. `main.dart` — `_showAdminHint()` removed, admin/owner callbacks hatae, "My Orders" tile ab real `OrdersScreen` khulta hai (pehle hint snackbar tha). NEW `screens/orders_screen.dart` — `ApiClient.fetchMyOrders(token, phone)` (query `?phone=`), RefreshIndicator, tap → `OrderTrackingScreen`, empty/error states, status colors. `menu_screen.dart` — `MenuScreen`+`TopFoodBar` se `onAdminClick/onOwnerClick` params + dono `_barIcon` buttons (admin_panel_settings, manage_accounts) removed; takeaway text → `'Usman Hotel Kitchen Counter (Ready in ~15 mins)'`. `session.dart` — defaults ab `deliveryAddress=''`, `selectedZone='My Location'`, `lat/lng=0/0`. `models.dart` — OrderEntity defaults `areaZone='My Location'`, lat/lng 0/0, fromJson zone fallback waisa. `cart_checkout_screen.dart` — pickup strings → `'Self-Pickup at Usman Hotel Main Counter'` (Model Town hata). `location_picker_sheet.dart` REWRITE — open hote hi `Geolocator.checkPermission→requestPermission` cap `getCurrentPosition(high, 15s)` auto GPS pin (denied → guidance snackbar), `_zones` hardcoded list REMOVED, map tap-to-move + FAB relocate, `Session.saveLocation()`.
- **Verify/build:** `flutter analyze --no-pub` **0 issues**, `flutter test` passes (boots-to-login finds 'USMAN HOTEL'). pubspec → **1.1.0+2**. Build 51.9MB → `D:\pos\UsmanHotel-CustomerApp.apk` (aapt: `com.usmanhotel.usman_order` v1.1.0 code 2 label "Usman Hotel").
- **Publish:** `node publish-update.mjs --user farhanadmin --pass Farhan123 --platform usman_customer --version 1.1.0 --build 2 --name PRISM --notes "..." --apk ...` → Metadata + APK OK (51.9MB). GET `/api/pos/update`: count=15, usman_customer latest `v1.1.0 b2 hasApk=true name=PRISM`.
- **Remaining:** Twilio env vars Railway par set karo taake REAL SMS chale (abhi fallback mode hai). Test order `03215550999` server par chhut hua (isolated — sirf us phone ko dikhta hai; admin se delete kar sakte hain).

## Task: Kotlin "Usman Hotel" → TWO new Flutter apps port + deploy — DONE (Sep 2026)
- **User:** port the reference Kotlin app (`C:\Users\usman\Downloads\usman_hotel_pos_2026.zip` → temp `uh-full` storyline wan) into SEPARATE Flutter apps — **Customer** (`com.usmanhotel.usman_order`, label "Usman Hotel") and **Admin** (`com.usmanhotel.usman_admin`, label "Usman Hotel Admin") — both on the SAME Railway server `/uh` subsystem (existing `uh.js` /uh module + API routes are live), each with its own in-app update channel.
- **Server:** `/uh` mounts at `/api/uh/*` on `https://usman-hotel-pos-server-production.up.railway.app`. Public: `GET /uh/menu` (auto-seeds 28 items), `POST /uh/auth/request-code`, `POST /uh/auth/login` (JWT 30d, fallback code 1234), `POST /uh/orders`, `GET /uh/orders`(?phone=). Manager-gated (`uhAuth`+`requireUhManager`): `GET /uh/admin/orders` (uh.js:324), `GET /uh/admin/users` (330), `PUT /uh/orders/:id/status` (344), `POST /uh/menu` (369), `PUT /uh/menu/:id` (394), `DELETE /uh/menu/:id` (413). Seeds: owner `03001234567` HOTEL_OWNER "Usman Hotel Owner", admin `03009876543` STAFF_ADMIN "Manager Kashif (Admin)"; customers `03214567890`/`03338877665`; all code `1234`; sample orders UH-7842 (OUT_FOR_DELIVERY, rider Tariq Mehmood), UH-5120 (TAKEAWAY, PREPARING). Auth: `requestCode` → code stored, `login` → {token,user}.
- **Customer app `D:\pos\usman_hotel_customer_app\`** (first-class clone): ported `LoginScreen.kt` (waiter/mode picker inline), `MenuScreen.kt` (empty-cart CTA, item sheets w/ size chips + "Extra Cheese +Rs120"/"Extra Sauce +Rs50"), `CheckoutScreen.kt` (COD hardcoded, address story/delivery field, summary, placing-order dialog), `OrderTrackingScreen.kt` (rider header 3 bubbles + live GPS chip, order details/summary + big Cancel/Delivered buttons, ₹/PKR), guest mode (`Skip` + guest-static order UH-1001 tracking), "update available" red ↔ green bounce button + system/update screens (UpdateService platform `usman_customer`). Guest mode session persistence + `assets/img/logo.png` (source `uh-full/ui/img/logo.png`) + icon set + `assets/img/menu/...` (12 PNGs, categic). Settings/address map: `geolocator`/`latlongs`/`flutter_map` wired; LocationPickerSheet → saved `cartLocation`/prefs.
- **Admin app `D:\pos\usman_hotel_admin_app\`** (robocopy clone of the WORKING customer app, minus build/.dart_tool): identity `com.usmanhotel.usman_admin`, MainActivity moved to `kotlin/com/usmanhotel/usman_admin/`, label "Usman Hotel Admin", update platform `usman_admin`. `api.dart` adds fetchAdminOrders/fetchAdminUsers/updateOrderStatus/toggleItemAvailability/addMenuItem; `models.dart` adds AppUser (+AdminSession/roleFromName in session.dart, AppColors in theme.dart). `screens/login_screen.dart` rewritten 2-tab (Staff Admin `03009876543` / Hotel Owner `03001234567`) — phone+code via ApiClient.requestCode/loginWithCode, role must be STAFF_ADMIN/HOTEL_OWNER. New `screens/admin_dashboard_screen.dart`: DashboardScreen.kt port — Live Orders tab (type filter chips ALL/🚚/🛍️ + status chips, filters `_orderStatusFilter/_orderTypeFilter`; order cards #id, TAKEAWAY/DELIVERY badge, status badge, Call button, customer/address/items/note, Total Rs PKR payment method, bottom = status actions row: Accept & Cook / Reject, Ready at Counter / Dispatch with Rider, Mark Delivered / Mark Collected); stats row (Total Sales, Active, Deliveries, Takeaways); Menu Inventory tab with availability Switch per item + **Add Deal dialog** (`_menuItemDialog`, shared top-level fn); owner portal button pushed to `OwnerDashboardScreen` (4 tabs: stock list w/ Urdu, customers list, staff list, all orders). `main.dart` = UsmanAdminApp → AdminShell → (login | AdminDashboardScreen), startup update SnackBar via UpdateService; logout in appbar leading.
- **Build gotcha:** Gradle wrapper MUST be **8.14.2** (NOT 9.3.1) with AGP 8.11.1 + Kotlin 2.2.20 — wrapper 9.3.1 broke the AGP 8.11.1 build (settings.gradle.kts identical across apps as the constraint). `flutter analyze --no-pub` clean 0 issues both apps; `flutter test` passes (boots-to-login).
- **APKs:** `D:\pos\UsmanHotel-CustomerApp.apk` (54,214,933 bytes, aapt: `com.usmanhotel.usman_order` v1.0.0+1 label "Usman Hotel") + `D:\pos\UsmanHotel-AdminApp.apk` (53,492,909 bytes, aapt: `com.usmanhotel.usman_admin` v1.0.0+1 label "Usman Hotel Admin"). Both app-release.apk under their build outputs.
- **Publish:** `node publish-update.mjs --user farhanadmin --pass Farhan123 --platform usman_customer --version 1.0.0 --build 1 --name PRISM --notes ... --apk ...` + same for `usman_admin`. GET verify: count=14, both platforms listed `v1.0.0 b1 hasApk=true`. Update screens in-app: customer + admin auto-check startup, SnackBar "New update available".
- **Note:** `usman_hotel_customer_app` was originally cloned from customer build then survived heavy screen edits; keep pod schema in sync (MenuItem/CartItem/OrderEntity + AppUser shared). Login fallback: any phone + code 1234 → role CUSTOMER (customer app) but admin app REJECTS role CUSTOMER (needs staff/owner). SDK path `C:\Android\Sdk` (aapt under build-tools, NOT default user path).

## Task: Good Lock dashboards — reference glass Due/Reserved cards + selected-pill colors — DONE (Sep 2026, v3.4.3 build 24 "PRISM")
- **User:** "Dash due/reserved order cards ko reference card banao dono dashboards (delivery + nashta, glk): round corners, transparent blackish-light, koi card bg na ho — cards float over screen/bubbles backdrop jaisa order-screen orders; date & filter pills selected par different colors; rider & orders area screen bg visible." Reference = `DashboardScreen.kt` (`OrderGlassCard`: PureGlassCard shape RoundedCornerShape(22), tintAlpha 0.64, **borderless**, no hard borders).
- **Delivery `dashboard_screen.dart` `_dashOrderCard`:** naya `glk = GlassTheme.isGoodLock`; `cardRadius = glk ? 22.0 : 12.0` (round corners 22); **bg ab glk → `Colors.transparent`** (card bg bilkul NAHI, backdrop/bubbles dikhta hai — pehle `0xFF1E1B18@0.64` dark-fill tha); border `Colors.transparent` (pehle se); shadow wg-branch black@0.18 blur 18 (floating soft). Baqi text/content (loc/addr/total/date/Status pill `_dashStatusPill`/items/Order #) waisa.
- **Nashta `dashboard_screen.dart` `_dueOrderRow`:** same — `glk` gate + `cardRadius = glk ? 22.0 : (wg ? 16.0 : 10.0)`, bg glk → `Colors.transparent`, border glk → transparent. Bracket gotcha: `color`/`border: Border.all(...)` nesting level badhne par `boxShadow`/`child` named-param errors — decoration block carefully re-indent kiya.
- **Date pills (dono dashboards `_buildDateFilter`):** `GlassChip` ab `glkGradient: [0xFF2979FF, 0xFF1565C0]` pass karta hai (reference blue gradient selected pill) — pehle sirf accent `0xFF2979FF`.
- **Verify:** `flutter analyze --no-pub` **0 errors / 59 issues** (baseline infos), build **59.8MB** (2 attempts — pehla 900s timeout, dobara 662s gradle) → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v3.4.3 code 24).
- **Publish:** pubspec `3.4.2+23` → `3.4.3+24`. `node publish-update.mjs --name PRISM --version 3.4.3 --build 24 --notes "..."` → Metadata + APK OK (59.8 MB). GET verify: latest `3.4.3+24` name=PRISM hasApk=true count=12.
- **Pending (next task):** order-card-level restyle for orders screens (glass radius 22, emoji badge, gradient status badge, amber total `GlkCardActionPill`/`GlkViewItemsToggle`) — user confirm kab bole.

## Task: Good Lock orders — reference header bar REMOVE — DONE (Sep 2026, v3.4.2 build 23 "PRISM")
- **User:** "reference header bar poora remove kro order screen sai" — Good Lock orders screens mein ab SIRF single `GlkLiveBanner` top par (badge + title + count/total + actions), standalone "Orders Screen" glass header bar ANAY NAHI.
- **Teeno apps (`delivery/nashta/order_taker` `_ordersScreen`):** header glass Container/Padding ab `if (!glk)` par render hota hai (non-goodlock wala original header waisa); glk branch fully REMOVE. `_glkOrdersHeader`/`_glkOtOrdersHeader` methods delete (ab unused).
- **`GlkLiveBanner` calls:** teeno ab `leading: const GlkOrdersBadge()` + subtitle `'{count} Orders • {total} PKR'` (count bhi banner mein move — pehle sirf total tha) + actions waisa (delivery calendar/search/select/close `_glkPillIcon`; nashta same + `if(_isManager())select`; order_taker refresh/select/close `_glkOtPill`).
- **Verify:** `flutter analyze --no-pub` **0 errors** (baseline infos hi), build **59.8MB** → `D:\pos\usman_hotel_app.apk`.
- **Publish:** pubspec `3.4.1+22` → `3.4.2+23`. `node publish-update.mjs --name PRISM --version 3.4.2 --build 23 --notes "..."` → OK. GET verify: latest `3.4.2+23` name=PRISM hasApk=true count=11.

## Task: Good Lock orders — compact merged top bar (header icons → live banner) — DONE (Sep 2026, v3.4.1 build 22 "PRISM")
- **User:** "orders screen header k date/search/select/close icons header se utho, live banner mein move karo; live banner k subLabel(date line)/amber total krahato — space kam, better looking. end vala version point + build number brhao." 
- **`glk_orders.dart`:** `GlkLiveBanner` REDESIGNED — ab combined single glass bar: `leading` (naya `GlkOrdersBadge` 38dp green circle) + `title`/`subtitle` left, `actions` (List<Widget> pill row) right; old `subLabel`/`dateLine`/`totalText` param GAYE (amber total pill + date/time line bhi removed). Same glass gradient bg `0x55...`, radius 20, white border 0.16, shadow.
- **Headers (teeno apps) slim:** `_glkOrdersHeader`/`_glkOtOrdersHeader` ab sirf badge + 'Orders Screen' + `$count Orders • X PKR` render karte hain (saare action pill icons hata diye). Delivery/nashta `_glkPillIcon` helper waisa (horizonally ab banner actions mein use hota hai); order_taker `_glkOtPill(Widget child, onTap)` naya helper (refresh spinner/select/close banner actions ke liye).
- **GlkLiveBanner calls (teeno apps):** delivery `title 'Delivery'`, subtitle `{_fmtNum(totalOrders)} PKR`, actions = calendar/search/select/close (`_glkPillIcon`, same colors 0xFF0EA5E9/0xFF2979FF&0xFFA1A1AA/0xFFFF8A80); nashta `title ordersView`, actions = calendar/search/`if(_isManager())select`/close; order_taker `title ordersView`, actions = refresh(`_refreshOrdersOnly(showSpin:)`)/select/close (`_glkOtPill`). Bulk bar order: `if (selectMode)` baad subTabs/views waisa.
- **Gotcha:** delivery `_ordersScreen` ki oneline `final tzOff = esc.timeZoneOffsetFor(...)` ab unused (banner dateLine use karta tha) → remove kiya; `currentSubLabel` dono apps me views row me abhi bhi use hota hai (delivery 3754, nashta 3661) — delete NAHI karna.
- **Verify:** `flutter analyze --no-pub` **0 errors** (baseline infos hi), build **59.8MB** → `D:\pos\usman_hotel_app.apk`.
- **Publish:** pubspec `3.4.0+21` → **`3.4.1+22`** (ENERGY point +1 per policy — "end vala version point"). `node publish-update.mjs --name PRISM --version 3.4.1 --build 22 --notes "..."` → Metadata + APK OK. GET verify: latest `3.4.1+22` name=PRISM hasApk=true count=10.

## Task: Good Lock orders screens — reference layout port (all 3 embedded apps) — DONE (Sep 2026, v3.4.0 build 21 "PRISM")
- **User:** "Good Lock theme ka reference orders screen layout teeno embedded apps (delivery/nashta/order_taker) mein same banao" — reference = zip `C:\Users\usman\Downloads\order-dashboard (2).zip` → temp `order-dashboard2` → `OrdersScreen.kt` (source of truth; model screenshots nahi dekh sakta). Card-level restyle baad mein (pending), is task = header/banner/pills top-bar layer.
- **Naya shared file `usman_hotel_app/lib/embedded/glk_orders.dart`:** `glkStatusGradient(key)` (reference gradients — active 00C853→009624, assigned 7C4DFF→512DA8, paid 00E676→00B0FF, due FF9100→FF6D00, cancelled FF1744→D50000, reserved 9C27B0→673AB7, `new`/`pay_later`→00C853→009624, `served`→00B0FF→0091EA); `GlkPill` (selected = gradient + white bold, unselected = dark glass `0xFF151310` α + white 0.1 border, icon/count support); `GlkLiveBanner` (glass banner: title + subLabel + dateLine left, amber total pill right); `GlkCardActionPill`; `GlkViewItemsToggle`. koi glass_theme import nahi (withValues alpha LinearGradient par NAHI chalta — `undefined_method` error → gradient colors par alpha hex `0xAA...` laga do).
- **Delivery (`delivery/order_taker_screen.dart`):** import + `_ordersScreen` mein `glk` branch — `GlkLiveBanner` (title 'Delivery', currentSubLabel, `${dayName.toUpperCase()} • ${shortDate} Live Orders` via `tzOff = esc.timeZoneOffsetFor(btSettings['receiptTimeZone'])`, total `_fmtNum(totalOrders) PKR`); **always-visible gradient GlkPill row** with `tabCounts` map (search hidden-views-row branch sirf `!glk`). `subTabs` reserved entry → plain `'Reserved'` (count glk pills se append hota hai; non-glk collapsed pill `t[0]=='reserved'` par manual append). `_ordersRiderFilterBar` glk branch: per-rider counts from `visibleOrders` (rides list mein `(count)` label), "All Riders (N)" blue gradient `00B0FF→0091EA` + `Icons.groups`, rider pills green→blue `00E676→00B0FF` + `Text(r)` icon; non-glk GlassFillChip waisa.
- **Nashta (`nashta/order_taker_screen.dart`):** import + `String ordersRiderFilter = ''` field (~262) + `filteredVisibleOrders` rider-filter (deliveryAgent trim match); `_glkOrdersHeader()/_glkPillIcon()` helpers (38dp `0x3300E676` circle + delivery_dining, "Orders Screen", `$count Orders • ${_fmtNum(total)} PKR`, actions calendar 0xFF0EA5E9 / search 0xFF2979FF&0xFFA1A1AA / checklist select / close 0xFFFF8A80); `GlkLiveBanner` (title ordersView, subtab, date via `tzOffset` getter ~275); always-visible glk GlkPill subTabs row; `_nashtaOrdersRiderBar()` (glk && ordersView=='Delivery'): All Riders blue gradient + per-rider counts. **GOTCHA:** `_bulkBar` edit ke waqt uski body (Column/Row/Wrap) ud gayi thi → `_nashtaOrdersRiderBar` calling ke saath body bhi restore kiya.
- **Order Taker (`order_taker/order_taker_screen.dart`):** import + `_ordersScreen` header glk → glass card (radius 20, gradient `0xAA161311→0xAA0F0D0B`, border white 0.18) + naya `_glkOtOrdersHeader()` (38dp circle + delivery_dining, "Orders Screen", `$count Orders • total PKR`, actions refresh `_refreshOrdersOnly(showSpin:)`/select toggle + `_selectedOrderIds.clear()`/`_closeOrders`); `GlkLiveBanner` (title ordersView, subtab, date via `_tzOffset()`, total = `orderTotals(o)['total']` sum); glk gradient GlkPill subTabs row (pay_later/new/served/due/paid/cancelled ab `glkStatusGradient` handle karta hai); non-glk original pills `if (!glk)` par. riders nahi.
- **Verify:** `flutter analyze --no-pub` **0 errors** (58 infos/warnings), build **59.8MB** → `D:\pos\usman_hotel_app.apk` (pubspec `3.4.0+20` → `3.4.0+21`; version bump ke baad REBUILD zaroor kiya).
- **Publish:** `node publish-update.mjs --user farhanadmin --pass Farhan123 --name PRISM --version 3.4.0 --build 21 --notes "..."` → Metadata + APK OK (59.8 MB). GET verify: latest `3.4.0+21` name=PRISM hasApk=true count=9. **Pending (next task):** order-card-level restyle (glass radius 22, emoji badge, gradient status badge, amber total, location pin 0xFFFF5252, rider line 0xFF40C4FF, "View items (N)" + `GlkCardActionPill`/`GlkViewItemsToggle` ab maujood hain) — user confirm kab bole.

## Task: Good Lock UPDATED — nashta `_orderCard` glk fix + republish v3.4.0 build 20 — DONE (Sep 2026)
- **User:** "usman hotel app read kro aur jo update rehit hai kr k udpated do mujy" — read app, find pending edits, finish & republish.
- **Pending:** 4 files edited AFTER last APK build (glass_theme 17:37, delivery dash 17:37, delivery ot 19:15, nashta ot 19:21). nashta `order_taker_screen.dart:3896` used `glk` in `_orderCard` (amber total `0xFFFFD54F` Good Lock branch) but never declared it → `undefined_identifier` error (half-finished Good Lock port).
- **Fix:** `final glk = GlassTheme.isGoodLock;` add in nashta `_orderCard` (after `glow`/before `wg`). Verify port complete: nashta now has `_cartFab/_typeTabs/_categoryRail/_productGrid/_hdrBtn/_searchBar/GlassBackdrop` all = delivery counts; `glass_theme.dart` `isNewGlass = glassyReference` (mode 3∨4) → nashta dashboard auto-gets reference look, delivery dash has explicit `isGoodLock` gates (7 refs) + `GlassGlow`.
- **Verify:** `flutter analyze --no-pub` **0 errors** (53 baseline infos), build **59.7MB** → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v3.4.0 code 20).
- **Publish:** pubspec `3.4.0+19` → `3.4.0+20`. `node publish-update.mjs --name PRISM --version 3.4.0 --build 20 --notes "..."` → Metadata OK + "APK uploaded OK: 59.7 MB". GET verify: latest `3.4.0+20` name=PRISM hasApk=true count=9. (`--user farhanadmin --pass Farhan123`.)

## Task: Good Lock theme — full reference POS layout (all 3 embedded apps) — DONE (Sep 2026, v3.4.0 build 19 "PRISM")
- **User:** "pos screen dashboard order screen gb aur icones b actions byuttons b same layout" — collection of prior zoomed-in reference folder screenshots. Reference = `C:\Users\usman\Downloads\order-dashboard (2).zip` → temp `order-dashboard2` (Kotlin/Compose). Design vars: bg `0xFF0F0E11`, card tint `0xFF141210`@0.55 radius 22 borderless + white sheen, orange `0xFFF9531E`/`0xFFF96432`, blue `0xFF2979FF`, amber `0xFFFFB300`, green `0xFF00E676`, red `0xFFFF334B`, text white/`0xFFD4D4D8`/`0xFFA1A1AA`; reference POS = glass top bar + gradient emoji type pills + dark search pill + circular food art + floating checkout bar.
- **`glass_theme.dart`:** naya mode **`glassGoodLock = 4`** (+ `_modeKey` bound `m <= 4`, `_apply` sets dark for mode 4). **`isNewGlass` ab `glassyReference` (mode 3 YA 4) return karta hai** → saari 1.2 dashboard/orders styling Good Lock mein bhi auto-applies; `isGoodLock`/`glassyReference` getters. Palette getters (`bgColors`, `overlayColors`, `backdropImage`, `glassTint/Alpha/Border/Sheen*`, `screenBg`, `insetBg`, `textPrimary/Secondary/Dim`) ab `glassyReference` se gate hote hain. Naye accent consts: `akOrange` `0xFFF9531E`, `akBlue` `0xFF2979FF`, `akAmber` `0xFFFFB300`, `akGreen` `0xFF00E676`, `akRed` `0xFFFF334B`, `akGreenDeep`, `akText`, `akDim`.
- **Printer/settings — Good Lock tile teeno apps mein** (`delivery/nashta/order_taker` `printer_settings_screen.dart`): `GlassTheme.glassGoodLock` tile (`Icons.whatshot`) Glass Dark/Light/1.2 row ke saath, Classic ke upar; `_themeTile` gradient ab `glassGoodLock` branch (dark warm preview `[0xFF1A1613,0xFF0D0C0E]`); nashta/order_taker mein `glassNew`/`glassGoodLock` preview handles kiye (order_taker mein `_buildAppearanceCard()` + `_themeTile` naya add + `../glass_theme.dart` import). Selection via `ValueListenableBuilder<int>(GlassTheme.mode)`.
- **Delivery POS (`delivery/order_taker_screen.dart`):** `glk = GlassTheme.isGoodLock` gate. `build`: `Scaffold.backgroundColor = GlassTheme.screenBg` + `const GlassBackdrop()`. Naya **`_cartFab()`**: floating glass pill (BackdropFilter blur 12, dark `0xFF1C1917`@0.72→`0xFF0F0D0C`@0.85, white@0.55 border, radius 22) — orange circle 🛒 + "$count items · $total PKR" white bold + green gradient "Pay & Print" pill (non-goodlock → old `FloatingActionButton.extended`). `_header`: glass borderless bar — circular logo (dark circle + white@0.35 ring), username green `akGreen` + role dim, pill buttons Orders/Dash/Printer/Exit with emoji, red badge `akRed`. `_typeTabs`: selected = `akBlue`@0.9 pill radius 100 + blue border + white text; unselected dark glass + white70. `_searchBar`: dark glass pill, white text, blue-focused. `_categoryRail`: 76dp rail, 64dp circular emoji art (`akBlue`@0.92 selected + white ring + glow; unselected `0x33141210` + white@0.22 ring), label white/dim. `_productGrid`: `glk` → `0x33141210` borderless dark glass card (active `0x1A00E676` + green border glow), 82dp circle image, name white→`0xFFD4D4D8` gradient, price green gradient, top strip dark glass, qty badge **orange `akOrange`** circle. `dart:ui` import add (IndexFilter context ke liye). Analyze **0 errors**.
- **Nashta POS (`nashta/order_taker_screen.dart`):** same full port (build + `_cartFab`, `_header`, `_typeTabs` vertical column pills, `_searchBar`, `_categoryRail` circular rail, `_productGrid` 96dp circle image). `dart:ui` import add.
- **Order Taker embedded (`order_taker/order_taker_screen.dart`):** same port (build + `_cartFab`, `_header` with circular logo + pill buttons, `_typeTabs` with blue pills, `_searchBar`, `_categoryRail` circular 76dp rail, `_productGrid` — name/price via ShaderMask gradient now `glk`-aware white/green); **`_ordersScreen`** glass shell (dark glass Scaffold + `GlassBackdrop`, translucent header bar, views/subtabs translucent when unselected + white70 text, `_bulkBar` translucent; order cards themselves unchanged dark). `../glass_theme.dart` + `dart:ui` imports add. Analyze **0 errors**.
- **Publish:** build 59.7MB → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v3.4.0 code 19). pubspec `2.4.0+18` → **`3.4.0+19`** (MAJOR bump per policy — new theme/icons/layout everywhere, funny name PRISM). `node publish-update.mjs --name PRISM --version 3.4.0 --build 19 --notes "..."` → "Metadata OK", "APK uploaded OK: 59.7 MB". GET verify: latest `3.4.0+19` name=PRISM hasApk=true count=9 = 8 previous + new.
- **Gotchas:** (1) `const EdgeInsets.symmetric(vertical: glk ? 8 : 10)` → invalid_constant (non-const glk in const context) — `const` remove. (2) `isUrduCategory` `order_taker` mein undefined (delivery/nashta ke pass hai) — glk rail mein Noto Naskh Arabic ternary drop. (3) `_ordersScreen` tail braces: Stack wrap se +1 `])` chahiye (Column zeapes Stack children). (4) order_taker embedded ka apna `_hdrBtn`/`_iconBtn` + separate orders screen (not the delivery/nashta one) — dono alag restyle hue. (5) cart sheets intentionally existing light look par rakhne.

## Task: Glassy Theme 1.2 reference-dashboard restyle (delivery + nashta dashboards) — DONE (Sep 2026, v2.4.0 build 18 "TRIO")
- **User:** "Enhancement wali update mein glassy theme 1.2 dashboards ko reference order-dashboard zip jaisa banao" — reference = `C:\Users\usman\Downloads\order-dashboard.zip` (Kotlin/Compose glassy dashboard). Apply **as-is layout** into Glass Theme 1.2 (`glassNew`/`isNewGlass`) — NOT a new mode.
- **Design language (from `DashboardScreen.kt`/`GlassComponents.kt`):** bg `0xFF0D0C0E`, **borderless pure-glass cards** (tint `0xFF141210` ~0.58, radius ~22, vertical white specular sheen top 0.34→bottom 0.06), text white/`0xFFD4D4D8`/`0xFFA1A1AA`, blue `0xFF2979FF` selected pills, orange `0xFFF9531E` Due, purple `0xFF7E57C2` Reserved, amber `0xFFFFB300` luminous glow behind revenue, warm bokeh bg photo.
- **`glass_theme.dart` changes:** `bgColors` newGlass → `[0xFF0D0C0E,0xFF11100F,0xFF161311]`; `overlayColors` → `[0xFF141210,0xFF262321]`; `glassTint` → `0xFF141210`, `glassAlpha` → 0.58, `glassBorder` → **transparent**, `glassSheenTop/Bottom` → white 0.34/0.06; `screenBg` → `0xFF0D0C0E`, `insetBg` → `0xFF12100E`; text → white/`0xFFD4D4D8`/`0xFFA1A1AA`. **Naya `backdropImage` getter** (`glass_bg.jpg` vs `bbq_bg.jpg`) + **`GlassBackdrop` ab const constructor returns, `image` param optional → build mein `image ?? GlassTheme.backdropImage`** (initializer-constraint se bachne ke liye). **Naya `GlassGlow` widget** (radial warm glow BEHIND child, renders only in newGlass).
- **Asset:** zip `app/src/main/res/drawable/img_glass_bg.jpg` → `assets/img/glass_bg.jpg` (596KB, pubspec assets/img/ already covers).
- **Delivery `dashboard_screen.dart`:** header card radius 22 + title 21 bold + `0xFFA1A1AA` date + red ✕ circle `0x1FFF334B`; `_buildDateFilter` radius 22 label 15 w600 GlassChip accent `0xFF2979FF`; `_buildRidersPanel` radius 22 borderless; **`_riderRow` = reference look** — 42dp dark-glass circle avatar (`0x33424242` + white top-left sheen), person `0xFF90CAF9` / unassigned help `0xFFFFB300`, name 16 bold, sub 12px5 secondary, **32dp circle badge** `0x4D333333` + thin white border, chevron `0x800A1A1AA` size 13; `_card` newGlass → GlassGlow amber + 34 w900; `_mini` newGlass → 108-height tile, 30 w900 top / 12 secondary bottom (spaceBetween); `_buildOrdersTabs` borderless + tabs Due `0xFFF9531E` / Reserved `0xFF7E57C2`; `_dashOrderCard` newGlass bg → `0xFF1E1B18` @0.64 (borderless warm glass, merged-tag path untouched); `_dateSep` newGlass → uppercase 12 bold secondary inside borderless GlassPanel pill.
- **Nashta `dashboard_screen.dart`:** same port — header (21 bold + red circle), `_buildDateFilter`/`_buildRidersPanel`/`_riderRow` (identical reference row), due panel radius 22 borderless, due badge 32dp circle, `_dueOrderRow` newGlass bg `0xFF1E1B18`@0.64 radius 16, `_card`+GlassGlow, `_mini`→108 tile.
- **Gotchas:** (1) `GlassBackdrop` param+initializer `: image = image ?? GlassTheme.backdropImage` ka double-init compile error `field_initialized_in_parameter_and_initializer` → resolve inside `build`; constructor **`const` rakha** (4 call sites `const GlassBackdrop()` unchanged — no edit needed). (2) `const EdgeInsets.fromLTRB(GlassTheme.isNewGlass ? ...)` → invalid_constant (non-const terna in const context) — remove `const`. `verify` `flutter analyze --no-pub` **0 errors / 50 baseline infos**.
- **Publish:** build **59.6MB** → `D:\pos\usman_hotel_app.apk`; pubspec `2.3.4+17` → **`2.4.0+18`** (MINOR UI bump, name TRIO). `node publish-update.mjs` OK → **GET verify: latest 2.4.0+18 name=TRIO hasApk=true count=8**. Endpoint = `/api/pos/update` (bina `/api` ke GET web dashboard HTML deta hai).

## Task: BUGS push — Cashier Mark Paid options server-synced (multi-device) — DONE (Sep 2026, v2.3.4 build 17 "TRIO")
- **User:** "bugs update mein mark paid enable krny par cashier ko doosry mobile par button nahi show ho — fix karo."
- **Root cause:** `CashierOptions` (cashier_options.dart) sirf **local SharedPreferences** (`cashier_options_v1`) mein save hota tha → manager ke phone par ON kiya to cashier ke phone par kabhi nahi jaata.
- **Fix (server-sync via existing `PUT /settings` routes.js:1216):**
  - `cashier_options.dart`: naye helpers `cashierOptionsFromSettings(dynamic settings)` (server settings ke `cashierOptions` key se parse) + `cashierOptionsSettingsPayload(CashierOptions)` (`{'cashierOptions': o.toJson()}`).
  - Printer settings (delivery + nashta `printer_settings_screen.dart`): `OrderTakerPrinterSettings` par naya optional `String? token` param; `_save()` mein `saveCashierOptions` ke baad `ApiClient.send('PUT','/settings', token, body: cashierOptionsSettingsPayload(_cashierOpt))` try/catch (silent).
  - Order_taker dono (`delivery`+`nashta order_taker_screen.dart`): Printer push site `OrderTakerPrinterSettings(token: token)` (const hata); `_loadData` ka `settings` fetch (`results[3]`) par `cashierOptionsFromSettings` → `_cashierOpt` + `unawaited(saveCashierOptions(...))` — cashier phone par server wali permissions pref+UI dono mein apply. Dashboards pehle se `loadCashierOptions(ns)` read karte hain (pref ab server se updated hota hai).
- **Flow:** Manager Settings → Options → Mark Paid ON → Save → server `/settings.cashierOptions` update → har phone ka `_loadData` (20s silent refresh) wahi value deta hai → cashier `_showMarkPaidOrders()` TRUE.
- **Gotchas:** server `PUT /settings` pehle se auth+merge (routes.js:1216 `{...db.settings,...settings}`) tha — koi server change NAHI. Token printer settings par ho to hi push hota hai (const/launcher usages safe). Build number **+17** zaroori tha (2.3.4+16 already published tha; same code par phir update detection fail hota).
- **Verify:** `flutter analyze --no-pub` **0 errors** (50 baseline infos), build **59.0MB** → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v2.3.4 code 17). Publish `--build 17 --name TRIO` → GET latest 2.3.4+17 name=TRIO hasApk=true count=7.
- **Beta:** same change beta app (`usman_hotel_app_beta`) mein nahi kiya (production-first).

## Task: SPEED energy push — light catalogue + cache-first bootstrap + glass scroll smooth — DONE (Sep 2026, v2.3.4 build 16 "TRIO")
- **User:** "speed update... app bahut slow... categories/items pehly show load hon, main loading na ho, dashboard glassy slow hai, plugin lo speed wala." Teeno embedded apps (delivery/nashta/order_taker) + dono dashboards covered.
- **Light products:** teeno order_taker files ka products fetch ab `/pos/products?light=1` (koyi scene har jagah: nashta `_loadData` dono jagah, delivery `_loadData`, embedded ot already tha) → server photo base64 bhejne ke bajay sirf `photoUrl` deta hai → payload ~10x chhota = slow internet par POS fast. Dashboard dono (`delivery/dashboard_screen.dart:297`, `nashta/dashboard_screen.dart:192`) bhi `?light=1` (sirf `_prodCats` category-map ke liye products chahiye). **`_pimg(p)`** helper add (delivery + nashta) = photoUrl ?? photo fallback, sab product-grid/cart SmartImage usages `_pimg` par.
- **Cache-first bootstrap:** naya `_bootstrapFromCache()` teeno order_taker files mein (delivery/nashta/embedded ot) — initState mein `_loadData()` se PEHLE call; cached categories/products/orders/settings turant set + `initialLoading=false` (sirf jab fields empty hon) → main loading spinner nahi, items turant. `_loadOfflineSnapshot()` untouched (offline fallback waisa). Gotcha: repeated `(categories as List).isEmpty` casts warnings → `categories.isEmpty` (fields already `List<dynamic>`).
- **Delivery `_loadData` 2-phase:** critical (categories+products) pehle await → `initialLoading=false`, phir full 11-endpoint background (nashta/embedded ot already 2-phase the). `final cat/prod` futures re-used dono awaits mein (single network call).
- **Glass smooth scroll:** `GlassPanel` build mein `RepaintBoundary` wrap (`child: RepaintBoundary(child: BackdropFilter(...))` + 1 extra `),`) — har glass card apni reusable layer → dashboard/orders scrolling compositor-cached = smooth. BackdropFilter blur radius WASAI 20 (bracket closure careful: +1 nested level).
- **Verify:** `flutter analyze --no-pub` 0 errors / 50 baseline infos (pre-existing `_setBt`, `_role`, `app_permissions:38`, `nashta/main.dart:165` warnings). Build **59.0MB** → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v2.3.4 code 16). Publish `--name TRIO` (majors ke saath name waisa) → GET latest 2.3.4+16 name=TRIO hasApk=true count=7.
- **Beta:** not applied (production-first energy; beta already has local Railway host + app_permissions).

## Task: Update screen upgrade — "name" (funny genius name) + emoji/color + Old Version Updates list + Historical names backfill — DONE (Sep 2026, v2.3.3 build 15 "TRIO")
- **User:** "enhancement wali update mein name dikhao + old version updates ki list name/notes/date ke saath."
- **App (`usman_hotel_app/lib/update_screen.dart`):** `UpdateInfo` model + server response mein naya `name` field. `_UpdateScreenState` naye helpers: `_nameEmoji(String)` (trio→🎉, zigzag/turbo/bolt→⚡, hulk→💪, jolt→🔥, performance→⚡, bug→🐞, update/in-app→📦, default→🚀), `_nameColor(String)` (6-color palette by hashCode). **`_latestInfoCard(UpdateInfo)`**: naya white card inside "Already Latest" AND available-update state — name ka emoji + colored name, `vX.Y.Z (build N)`, Release Date & Time, File Size, notes box. **`_oldUpdatesButton()`**: history tile — "Old Version Updates (N)" → push naya `_OldVersionUpdatesScreen`. Records count includes ALL updates (sab purani), N = non-current ones. **`_OldVersionUpdatesScreen`**: full screen (sky-blue `0xFF0EA5E9` history icon header) — sab updates sorted buildCode desc (tie → publishedAt desc), har card par colored name + emoji, version/build, release date, size, hasApk cloud icon, notes. Empty state = placeholder tile. App-installed version ke baad wali updates ab bhi bana rhi hain.
- **Server (`updates.js`):** `publishUpdateMeta` ab `name` bhi store karta hai (body `name` || ''); **republish APK preserve fix**: existing record par `fileName/sizeBytes/apkUploadedAt` carry-over (pehle metadata republish par binary reset ho jata tha → downloadURL merra maloom nahi hota). `routes.js` POST `/pos/update` body destructure `name`. `publish-update.mjs` naya `--name` flag. `node --check` OK.
- **Pushed:** server commit `b4fd07e` (feat: update name field + preserve apk on republish) → Railway. App: analyze 0 errors (50 infos baseline), build **58.9MB** → `D:\pos\usman_hotel_app.apk` (aapt `com.usmanhotel.usman_hotel_app` v2.3.3 code 15). Publish `--name TRIO` → verify GET latest 2.3.3+15 name=TRIO hasApk=true count=6. **Backfill:** node temp script (login→POST metadata per old version): 2.3.2→`Tidy`(+14), 2.3.1→`Retro`(+13), 2.3.0→`TRIO`(+12), 1.3.0→`Breeze`(+11), 1.2.0→`Spark`(+10) — sab hasApk=true aur sizes preserved. GET verify: 6 records sabsaath names.
- **Gotcha (deploy verify):** Railway deploy live hone ki check `GET /pos/update` par `name` field — purane deployed code mein `name` response mein nahi tha, records bhi wahi the → "deploy still old 26/19" poll loop galat laga tha. Asli deployment poochna: `node publish-update.mjs --name ...` publish kar ke GET par name aa jaye (2.3.3 TRIO live title). Deploy live tha hi.
- **Beta:** ye changements beta app (`usman_hotel_app_beta`) mein abhi apply NAHI kiye (production-first energy push).

## POLICY (hamesha follow karo): Usman Hotel App — UPDATE INSTRUCTIONS / VERSIONING (Sep 2026)
- **Current version:** `v1.3.0` (build 11). Version format = **`MAJOR.MINOR.ENERGY`**; **build number HAMESHA barhta hai** har push par (11 → 12 → 13 …).
- **1) MAJOR update — pehla number** (`1.3.0` → `2.3.0`): sab se bari update. Isi mein **random funny "genius" name** (one word, team/AI khud se detay hain). Is mein **5+ changes** hote hain (10–12+ ideal): UI/spacing, performance, buttons design, functions, screens, themes. **Major update ONLY 10 din ke baad** last major ke (min gap 10 days), us se pehle NAHI. Is mein Bugs Controller + Security update bhi shaamil hoti hai. **Workflow:** roz ke chotay changes beta file mein log karo + local save karo (push NAHI); jab 10–12+ changes bhar jayein aur sab verify+test OK hon → tab EK saath major push.
- **2) MINOR update — beech wala number** (`1.3.0` → `1.4.0`): sirf **1–2 chotay UI changes** (buttons aage/peeche, colors, text). Baqi sab (name/build) major ke saath WASAI rehta hai — koyi naya name nahi. Koi time restriction nahi — jab chahe push.
- **3) ENERGY PUSH (performance) — aakhri number** (`1.3.0` → `1.3.1`): sirf app speed/performance barhane wali — **3–4 speed improvements** (refresh speed, orders/screens syncing, smoothness, screen opening animations, more efficient). Naam major ke saath waisa. Koi time restriction nahi.
- **4) BUGS CONTROLLER:** crashes, bugs, cached-data problems fix + cached data cleanup — **HAR update mein lazmi shaamil** (4th = small category, same as 2/3, koi restriction nahi).
- **5) SECURITY UPDATE:** security enhance karo + update notes mein user ko batao (kaunsa security add hua + kitni safety hai). **Must push har ~30 din** — new month ki **2nd–3rd date** par (zaaroori). Last security ke **30 din ke baad** hi dete hain. Major update ke andar bhi security shaamil ho sakti hai.
- **Restrictions summary:** MAJOR = min 10-din gap; SECURITY = min 30-din gap. MINOR / ENERGY / BUGS = jab bhi chahe (2–3 chotay changes ho to EK hi choti prompt-update push karo — har choti cheez alag ALAG push NAHI).
- **Beta file:** `D:\pos\usman_hotel_beta_changes.md` — pending/unverified chotay changes wahan log hote hain (date, type, changes, status). Jab verify+test OK → us list ko Major-Update list promote karo → asli update push.
- **Naam examples (major ke liye):** Aik random one-word funny/genius name (e.g. "Zigzag", "Jolt", "Turbo", "Hulk", "Bolt") — version ke saath notes mein likho.

## Task: Beta APK copy of Usman Hotel app (alag project) — changes pehly beta mein, verified → production major — DONE (Sep 2026)
- **User:** Usman Hotel ki **poori APK ki beta copy** chahiye — naye changes PEHLY beta app mein apply/testing karo, verify+OK hone par hi production app mein **major update** push. 
- **Beta project:** `D:\pos\usman_hotel_app_beta` (poora copy of `usman_hotel_app`, robocopy se build/.dart_tool/.gradle chhod kar). **`applicationId = com.usmanhotel.usman_hotel_beta`** (build.gradle.kts:31) + manifest label `"Usman Hotel Beta"` — isi liye production + beta EK phone par sath-sath install ho sakte hain. Build → `D:\pos\UsmanHotel-Beta.apk` (**56.9MB**, v1.3.0 build 11). Verified via `aapt dump badging`: package `com.usmanhotel.usman_hotel_beta`, label Usman Hotel Beta. Same release keystore (`android/key.properties`) se signed.
- **FileProvider note:** beta ka FileProvider authority `${applicationId}.fileprovider` = `com.usmanhotel.usman_hotel_beta.fileprovider` (applicationId se khud change ho gaya). UpdateService platform abhi `usman_hotel` hi hai (production wala update channel).
- **Workflow (ab se):** koi bhi naya change pehle `usman_hotel_app_beta` mein karo → `flutter analyze` + build beta APK → user test kare. Verified ho to wahi changes `usman_hotel_app` (production) mein bhi apply karo → major update push (build +1, funny name).
- **Gotcha:** AGENTS emoji gotcha — repo commits sirf server/features ke liye; dono app projects uncommitted rehty hain.

## Task: Beta app = pure test bed — update screen remove + local DB only — DONE (Sep 2026)

> **UPDATE (19/09/2026) — login fix, beta ab RAILWAY host karta hai (ye section OVERRIDE):**
> User: "login par updated beta build do" — beta phone par local LAN server reachable nahi tha (10.231.26.192), is liye login fail. **Ab charo beta `api.dart` ka `defaultHost` = `https://usman-hotel-pos-server-production.up.railway.app`** (login/catalog/orders sab Railway se). Login creds = **production staff** (beta local creds Railway par nahi hain). Update screen wala removal + `main.dart` startup `prefs.remove('serverUrl')` + `setHost(defaultHost)` WASAI hai.
> **Order isolation (beta orders production mein NAHI dikhte, stock NAHI kat-ta):** teeno embedded apps (delivery/nashta/order_taker) ab orders ko **`source: 'usman_hotel_beta'`** se tag + fetch `?source=usman_hotel_beta` karte hain. **Server routes.js (pushed `7282c31`):** `GET /pos/orders` beta sirf tab deta hai jab `source=usman_hotel_beta` explicit ho (warna exclude); `/dashboard` paidOrders par beta exclude; `POST /pos/orders` beta source par **stock deduction skip**. Production apps (apne sources se fetch) aur admin web dashboard beta orders nahi dekhte. **APK:** `D:\pos\UsmanHotel-Beta.apk` (61,339,720 bytes, aapt verified: `com.usmanhotel.usman_hotel_beta`, v1.3.0+11, "Usman Hotel Beta").
- **`launcher_screen.dart` (beta):** Update button block (`FilledButton.icon` "App Update — Check / Install", `UpdateScreen` push + `_maybeRefresh`) remove + `import 'update_screen.dart';` hata diya. Logout button + `initState` ka `_maybeRefresh()` waise hi rehta hai.
- **API host (beta, teeno files):** `lib/api.dart` + `lib/embedded/delivery/api.dart` + `lib/embedded/nashta/api.dart` — `defaultHost` ab **`http://10.231.26.192:4000`** (PC ka LAN IP, local server port 4000; `D:\pos\server` start karo: `npm run dev`) — Railway URL kahin nahi. `_shouldUseDefault` bhi simplify: ab sirf **empty/localhost** par default host use hota hai, warna jo type karo wahi host (pehle `10.*`/`192.168.*`/`172.16-31.*` sab ko default par reset kar deta tha → LAN IP type karne se Railway chala jata tha). Login screen ka server field (`advanced`) ab kisi bhi PC IP par kaam karta hai.
- **Data separation:** beta ka applicationId alag (`com.usmanhotel.usman_hotel_beta`) → SharedPreferences/cache pehle se sandboxed; ab server-side data (orders/settings) bhi local server par jaata hai. Phone + PC same WiFi hona zaroori; PC par local server chal raha ho.
- **Verify:** `flutter analyze --no-pub` beta = **0 errors**; `flutter build apk --release` ok (56.3MB) → copy `D:\pos\UsmanHotel-Beta.apk` (59.0MB ~ 59042937 bytes) — aapt: package `com.usmanhotel.usman_hotel_beta`, versionName 1.3.0 code 11, label "Usman Hotel Beta". Production app untouched (update screen + Railway host waisa hai).
- **Gotcha:** const default value mein semicolon **comment se PEHLE** rakhna — `'...'; // comment` (comment ke baad `;` lagao to expected_token ';' error aata hai, dono embedded api.dart mein hua).
- **Gotcha (bara — beta orders production mein dikhna):** beta app ka `main.dart` (aur embedded delivery/nashta main.dart) pehle `prefs.getString('serverUrl')` use karta tha — beta jaan bujh kar **Railway era** mein yeh pref save ho gaya tha → naya APK install hua to bhi pref override kar ke Railway ko hit karta tha → beta orders production mein aa gaye. Fix: beta teeno main.dart ab startup par `prefs.remove('serverUrl')` karte hain + `ApiClient.setHost(ApiClient.defaultHost)` hamesha; aur `_shouldUseDefault` ab `railway.app`/`rlwy` substring par bhi true (Railway URL type karo to bhi local default).** Koi bhi production deploy ho toh beta ka data production kabhi nahi jayega. (Jab purani Railway orders/URL test ke liye ja chuke hon, unhe production server/web dashboard se manually delete karna hoga — beta fix unhe wapas nahi laata.)

## Task: Order Taker app BETA mein embedded (vendored, launcher se 3rd app) — DONE (Sep 2026, beta-only)
- **User:** "Order taker app ko embed karna beta usman hotel mein" — launcher par teeno apps: Nashta / Delivery / **Order Taker** (amber `0xFFB45309` card). Ye **vendored copy** (path-dep/nested-MaterialApp wala approach REVERTED tha us se pehle) — workspace par pehle se kaam chala hua tha (embedded/order_taker files + launcher wiring), sirf 1 error rehta tha.
- **`lib/embedded/order_taker/`:** `api.dart` (defaultHost **LOCAL** `http://10.231.26.192:4000` + `_shouldUseDefault` railway.rlwy → local), `escpos.dart`/`bmp_receipt.dart`/`bt_service.dart`/`offline_store.dart` (standalone wale copy), `order_taker_screen.dart`, `printer_settings_screen.dart` (`class OrderTakerPrinterSettings`). Missing vs standalone: `dashboard_screen.dart`/`login_screen.dart`/`main.dart`/`session.dart` — embed hone par layout alag: header "Dash" button → **`OrderTakerPrinterSettings`** (`_hdrBtn` ⚙️ Printer), `handleLogout` = prefs clear ki jagah `Navigator.pop()` (launcher wapis).
- **Permissions (embedded specific):** `_perms` getters (`UserAppPerms.fromUser(user)` se table/takeaway tabs + orders + printer per-cashier toggles), `_tablePosAllowed/_takeawayPosAllowed/_tableOrdersAllowed/_takeawayOrdersAllowed/_printerAllowed`, `_isManagerLike` = `canManageRoles`. `_openOrders`/`_openPrinterSheet`/type-tabs/ordersViews sab perms-aware — manager ko off feature par `_permBlocked` snackbar. `app_permissions.dart` mein `AccessRolesCard` kiye `_role` ab `role ?? ''` (String? → String fix, line 207).
- **Launcher wiring:** `launcher_screen.dart` par 3rd card "Order Taker" (subtitle 'POS, orders, bluetooth print') — `_openOrderTaker()` gate = `roleAllowedInOrderTaker(role) && UserAppPerms.fromUser(_user).orderTaker`; push `ot.OrderTakerScreen(token,user)`. `session.dart` `orderTakerRoles` list ~ `app_permissions.dart` `orderTakerRoles` (Order Taker/Admin Order Taker/Waiter/Helper/Cashier/Manager/Admin).
- **Verify:** `flutter analyze --no-pub` **0 errors** (50 infos/warnings baseline), `flutter build apk --release` ok (**58.5MB**) → copy `D:\pos\UsmanHotel-Beta.apk` (61.3MB bytes) — aapt: package `com.usmanhotel.usman_hotel_beta`, v1.3.0 code 11, label "Usman Hotel Beta". Production app untouched (abhi sirf beta mein test).
- **Note:** Jis din verified ho → wahi embedded/order_taker files `usman_hotel_app` (production) mein aayengi next major mein (parallel edit policy). Test ke liye local server chahiye: `D:\pos\server` → `npm run dev` (port 4000), phone + PC same WiFi.
- **✅ PROMOTED & PUSHED (19/09/2026) — major v2.3.0 build 12 "TRIO":** beta embedded order_taker verified → production mein port kar diya. Production edits: `lib/embedded/order_taker/` (7 files) + `lib/embedded/app_permissions.dart` + `session.dart` (orderTakerRoles/roleAllowedInOrderTaker/ot-token persistence) + `main.dart` (ot_api host + onAuthError + `_decide` orderTaker gate + splash "NASHTA + DELIVERY + ORDER TAKER") + `launcher_screen.dart` (3rd Order Taker card + gate, **update button WASAI**) + delivery `printer_settings_screen.dart` (AccessRolesCard) + pubspec `image_picker ^1.1.2`. **Beta-specific (bcosRailway flip + `usman_hotel_beta` source tags) production mein NAHI liye** — production order_taker revert kiya: fetch `/pos/orders` (filterless), create `source: 'order-taker-app'`, `_filterForeignOrders` (nashta/bbq-exclude wala original) taake standalone `order_taker_app` se consistent rahe. Analyze 0 errors, APK 58.9MB → `D:\pos\usman_hotel_app.apk` (aapt: `com.usmanhotel.usman_hotel_app` v2.3.0+12), publish-update OK (latest 2.3.0+12, count 3). **Ab se beta + production embedded order_taker/files parallel maintain karo.**
- **✅ ENERGY/BUGS PUSHED (19/09/2026) — v2.3.1 build 13:** user reported Order Taker app mein **past orders nahi dikhte** production mein. Root cause: embedded order_taker `ordersDateRange` default `'today'` tha → Orders screen sirf aaj ke order dikhata tha. Fix: default `'all'` kiya (both production `usman_hotel_app` + beta `usman_hotel_app_beta` embedded ot) → past orders hamesha visible, date-chips se filter karta rahe. `flutter analyze` 0 errors, build 58.9MB, aapt `com.usmanhotel.usman_hotel_app` v2.3.1+13, publish-update OK (latest 2.3.1+13, count 4). Update notes = "Performance & Bugs".
- **✅ ENERGY/BUGS PUSHED (19/09/2026) — v2.3.2 build 13:** user requested (same P&B update, "build 13 hee rhy, endpoint 1→2"): (1) **Date filter** select karne par due/paid/pay-later/cancelled orders show hon — root cause: `_inOrdersDateRange` mein `createdAt` parse fail ho to `return false` → un-parseable-date orders date-filter par gayab. Fix: `return true` (unparseable/missing-date orders hamesha visible). (2) **Takeaway view se Types filter remove** — `_ordersTypeFilterBar()` ab sirf `ordersView != 'Takeaway'` par render; view-switch par `ordersTypeFilter=''` reset (stale cross-view merge bug bhi fix). Both production + beta embedded ot. Analyze 0 errors, build 58.9MB, aapt `com.usmanhotel.usman_hotel_app` v2.3.2+13, publish-update OK (latest 2.3.2+13, count 5). Notes = "Performance & Bugs".
- **✅ GOTCHA (19/09/2026) — build 13 duplicate → update detection fail:** user ne kaha "build 13 hee rhy" → do alag APKs build 13 par aaye (2.3.1+13, 2.3.2+13). Update screen `latest.buildCode > installedBuild` (update_service.dart:59) se compare karta hai → build 13 wale phone par nayi 2.3.2 show NAHI hui (13 > 13 false). **Fix: build number HAMESHA har push par +1** (AGENTS policy). **v2.3.2 build 14 republished** (version waisa, versionCode 14, aapt `com.usmanhotel.usman_hotel_app` v2.3.2+14), publish-update OK (latest 2.3.2+14, count 5). Ab build 12/13 wale phones update dekh lenge.
- **Beta login credentials (local Postgres `usman_hotel_pos` pos_data + db.json fallback):** Manager = `betamanager` / `manager123`, Cashier = `betacashier` / `cashier123` (dono loginEnabled + bcrypt, apps default all-on). User chahta tha Railway se login/catalog aur orders beta-only — lekin phir decide kiya: **sab local hi rahega** (Railway touched NULL), sirf local staff login chahiye tha. Ye din ke liye: naye staff add karne ka sahi rasta = server ka apna `db.js` use karo (`import './config.js'; import { initDatabase, getCollection, createRecord, waitForPersist } from './db.js'`) — **raw SQL UPDATE pos_data SET payload=$2::jsonb FAIL hota hai** (`could not determine data type of parameter`, jsonb blob bhi). Server `npm run dev` par Postgres mode chalta hai (`.env` DATABASE_URL → localhost:5432/usman_hotel_pos), `pos_data(id, payload jsonb)` single-row blob. Port 4000 par server band tha/rag started karke login verify kiya phir band kar diya.

## Task: Pull-to-Refresh (swipe to reload) dono embedded apps par — Orders / POS items / Categories / Dashboards — DONE (Sep 2026) + pushed as update v1.3.0 build 11
- **User:** usman_hotel_app ki dono apps (delivery + nashta) mein **upar se neechy swipe (pull-to-refresh)** karein to **orders + data + POS screen items + categories screens fast reload** hon. Update ka naam **"Performance Enhancement"**.
- **4 files edited (delivery + nashta `order_taker_screen.dart` + dono `dashboard_screen.dart`):** `RefreshIndicator` wraps added.
  - **POS main screen:** `Expanded(child: initialLoading ? ... : Row(categoryRail, productGrid))` → Row ab `RefreshIndicator(color: green, onRefresh: () => _loadData(silent: true))` ke andar (categories + products fast reload). Dono mein `_categoryRail` ListView + `_productGrid` GridView par `physics: AlwaysScrollableScrollPhysics()` add kiya (chhota content ho to bhi pull chale).
  - **Orders screen:** `Expanded` list ab `RefreshIndicator(onRefresh: _refreshOrdersOnly)` wrapped — empty case ab scrollable `ListView` (pehle plain Center, pull na chalta) + `ListView.separated` par physics. `_refreshOrdersOnly` = 1-endpoint orders refresh + pending sync trigger + green/red sync indicator — perfect "fast reload".
  - **Dashboards (dono):** AnimatedSwitcher ke andar `child: ListView` → `RefreshIndicator(onRefresh: _refreshOrders, color: sky 0xFF0EA5E9)` wrap (dashboard cache+refresh). **Bracket gotcha:** AnimatedSwitcher+RefreshIndicator+ListView = +1 closing `),` — dono bar ek extra `),` add ho gaya tha → `missing_identifier/expected ']'` errors at dashboard build; ek `),` hata kar fix.
- **Verify:** `flutter analyze --no-pub` **0 errors** (baseline infos/warnings hi — `_setBt` unused marker check karna bhoolna mat), `flutter build apk --release` ok (**56.9MB**, incremental 230s/470s). Copy → `D:\pos\usman_hotel_app.apk`.
- **Push:** pubspec `1.2.0+10` → `1.3.0+11`; `node publish-update.mjs ... --version 1.3.0 --build 11 --notes "Performance Enhancement - Orders, POS items & categories: upar se neechy swipe kar ke fast refresh karein!" --apk D:\pos\usman_hotel_app.apk` → "Metadata OK -> id=usman_hotel_1.3.0", "APK uploaded OK: 56.9 MB". GET verify: `latest: 1.3.0 build 11 hasApk true count 2` (1.2.0 + 1.3.0 dono server par; phones build 1 se => update available).
- **No new server code** — sirf app + publish. Railway commit/push is for app files only if needed (AGENTS emoji gotcha: repo mein commits sirf server/features ke liye).

## Task: In-app Update system (launcher "App Update" button → check/download%/install; server-pushed APK) — DONE (Sep 2026)
- **User:** usman_hotel_app launcher (Delivery+Nashta selection) par **Update button** chahiye — press par screen kholo, **new update version/date/time** dikhao, **Download** karo **percentage + MB** ke saath, phir **Install** → app khud restart ho kar update install ho jaye. Updates **server se push** hon (har phone par APK bhejna na parre); agar koi update push nahi hui to **"Already Latest Version"** dikhao.
- **Server (`D:\pos\server`):** naya `updates.js` module — Postgres table `app_updates` (id TEXT PK = `<platform>_<version>`, platform, version, buildCode, notes, fileName, apk BYTEA, sizeBytes, apkUploadedAt, publishedAt; local-JSON mode fallback `server/data/updates/<id>.apk`). `db.js` par sirf `getPg()`/`isPgConnected()` exports. Routes (`routes.js`, block **BEFORE `router.use(authenticate)`** — warna public GETs 401): `GET /api/pos/update` (public, `{latest,count,updates}` buildCode-desc), `POST /api/pos/update` (Manager/Admin/**Admin Order Taker** = `isManagerOrAdmin()`, lowercased role check), `PUT /api/pos/update/apk/:id` (auth, raw `application/octet-stream`), `GET /api/pos/update/apk/:id` (public stream, `Content-Length` set + `Content-Encoding: identity` taki app progress bar chale - compression band). `index.js`: `express.raw({type:()=>true, limit:'250mb'})` on `/api/pos/update/apk` **BEFORE** `express.json` (10mb limit bypass) + `await initUpdatesTable()` after initDatabase. `server/publish-update.mjs` CLI: `--host --user --pass --version --build --notes --apk` (login `/api/auth/login` → POST meta → PUT raw APK).
- **App (`D:\pos\usman_hotel_app`):** pubspec `1.2.0+10` + deps `path_provider ^2.1.5`, `package_info_plus ^8.3.0`, `android_intent_plus ^6.0.0`. **Naya `lib/update_service.dart`**: `UpdateInfo` model (id/platform/version/buildCode/notes/fileName/sizeBytes/hasApk/downloadUrl/publishedAt→local time), `fetch({installedBuild})` → GET `${ApiClient.base}/pos/update` filter platform=='usman_hotel' (teeno plugins ka lib: android_intent_plus ka import path hai `package:android_intent_plus/android_intent.dart`, **`android_intent_plus.dart` NAHI** - `uri_does_not_exist` error deta hai), `downloadApk(url,dest,onProgress)` (http streaming, Content-Length se %, chunks se progress), `requestInstall(packageName,fileName)` (ACTION_VIEW + `application/vnd.android.package-archive` + `content://<pkg>.fileprovider/apk/apk_update/<file>` + `Flag.FLAG_GRANT_READ_URI_PERMISSION`, `canResolveActivity()` guard), `apkStorageDir()` = `<filesDir>/apk_update/`, `formatSize`/`formatDateTime`. **Naya `lib/update_screen.dart`**: `_UpdateStatus` enum (checking/error/noUpdate/available/downloading/ready/installing) — installed version card, green "Already Latest Version" card (agar server record ho to uski version bhi dikhati hai), update card (version bada, Release Date & Time via publishedAt, File Size MB, notes, Download buttin `Download Update (x.x MB)`, download progress % + `x.x MB / y.y MB` LinearProgressIndicator, phir **Install Update** button), install ke baad auto re-check + SnackBar "Update install ho gayi...". Errors: check fail / download fail / installer can't open → "Settings → Install unknown apps → Usman Hotel allow" hint. **`launcher_screen.dart`**: Logout ke upar full-width green FilledButton "App Update — Check / Install" → push UpdateScreen, return par `_maybeRefresh()`.
- **Android manifest:** `REQUEST_INSTALL_PACKAGES` permission + FileProvider `<provider android:name="androidx.core.content.FileProvider" android:authorities="${applicationId}.fileprovider" ... meta-data @xml/file_paths>`; naya `android/app/src/main/res/xml/file_paths.xml` = `<files-path name="apk" path="."/>` (filesDir root share; URI path = `/apk/<relative-path-under-filesDir>`).
- **Version strategy:** pubspec `1.2.0+10` (pehle committed `1.0.0+1`) → phones par installed buildNumber=1, server build 10 → update available dikhta hai. App compare: `latest.buildCode > installedBuildNumber`.
- **Deploy/verify:** `flutter analyze --no-pub` **0 errors** (baseline infos hi), `flutter build apk --release` ok (**56.7MB**, pehli build 11min: naye plugins ka Gradle download). Copy → `D:\pos\usman_hotel_app.apk`. Server: commit + `git push origin main` (Railway auto-deploy) **pehle**, phir `node publish-update.mjs ... --user farhanadmin --pass Farhan123 --version 1.2.0 --build 10 --notes "..." --apk D:\pos\usman_hotel_app.apk` → "Metadata OK -> id=usman_hotel_1.2.0", "APK uploaded OK: 56.7 MB". GET verify: `{"latest":{...,"buildCode":10,"hasApk":true,"downloadUrl":"/pos/update/apk/usman_hotel_1.2.0"}}`; download GET = 200, Content-Length 59502293, Content-Encoding identity.
- **Gotchas:** (1) `farhanadmin/Farhan123` ka role Railway par **Admin Order Taker** hai → `isManagerOrAdmin` strrict matching 403 deta tha; user ne **Admin Order Taker ko allow** karna approve kiya (2nd commit `ca92fe9`). (2) android_intent_plus import path (`android_intent.dart`). (3) update routes authenticate gate se PEHLE hone chahiye. (4) Apollo/bash web request: `Invoke-WebRequest` NonInteractive prompt deta hai — `node -e "fetch(...)"` use karo. (5) Local smoke record `smoke_0.0.1` pg se delete ho gaya tha; local server restart par cache clear.

## Task: Glassy Theme 1.2 refine round 2 — order-screen cards pure glassy transparent (mode 3): all card bg/border colors removed, only status tags coloured, rest text white + dashboard opening smooth/fast via cache — DONE (Sep 2026)
- **User:** dono apps (delivery + nashta takeaway/table) k order screens ke har tab ke order cards ka **background aur border color FULL REMOVE** — pure glassy transparent. Sirf **rider icon + status tags** colours mein rahen (paid/due/active/assigning/reserved/cancelled/delivery) apne existing colors ke saath; **baaki saara text white**. Dashboard opening bhi **fast + smooth** chahiye.
- **Order cards (`delivery/order_taker_screen.dart` + `nashta/order_taker_screen.dart` `_orderCard`):** naya `final wg = GlassTheme.isNewGlass`. **wg branch:** GlassPanel `alpha: 0.0` (fully transparent, backdrop photo dikhta hai), `borderColor: Colors.transparent`, outer boxShadow → neutral `Colors.black@0.18` (glow color HATA, spread 0). Address/loc/subtitle/time/total/Order#/items sab **white** (`Colors.white`/`white70`) — reserved-tab red glow, blue address glow, green delivery-fee, purple loc icon sab wg mein white ho jate hain. `_orderDetail` wg → bg `Colors.transparent` + white text, border `null`.
- **Naya `_statusTagPill()` helper (dono files):** mode-3 mein har card ke top Row mein `_deliveryPill()` ke saath ek **coloured status tag pill** dikhata hai — `active`→ACTIVE blue (`0xFF2563EB`, `Icons.play_circle_fill`), `assigned`→ASSIGNED purple (`0xFF7C3AED`, `Icons.two_wheeler`), `due`→DUE amber (`0xFFF59E0B`, `Icons.schedule`), `reserved`→RESERVED red (`0xFF991B1B`, `Icons.lock_outline`), `cancelled`→CANCELLED red (`0xFFDC2626`, `Icons.cancel_outlined`), default→PAID green (`0xFF059669`, `Icons.check_circle`). Nashta mein extra `payLater`→PAY LATER (`0xFFB45309`). Pill = color border + color@0.14 fill + color icon/text 9pt w900. Non-wg → `SizedBox.shrink()`. Delivery pill (`_deliveryPill`/`_typePill`) sab cards par har tab mein waisa colourish raha.
- **Dashboard opening smooth + fast (dono dashboards):** initState mein ab `OfflineStore.namespace` set + `OfflineStore.loadCache()` se pasted orders **turant render** (pehle blank thi jab tak network response na aaye) phir `Future.microtask(_refreshOrders)`. `_refreshOrders` ke outcome par `OfflineStore.saveCache({'orders': out})`. Build ka `Expanded(ListView)` → `AnimatedSwitcher(duration 450ms, key: ValueKey(_orders.isEmpty ? 'loading':'loaded'))` — cached/loaded data par smooth fade-in. Nashta dashboard ke initState mein `_refreshOrders` **pehle tha hi nahi** (sirf rider-sheet close par chalta tha) — ab add kiya (bukhar dismiss, ab open hote hi cache+refresh). OfflineStore import dono dashboard files mein add kiya.
- **Dashboards (dono) transparent-black cleanup:** `_miniBtn` wg → `white@0.16` + white border (pehle solid green `0xFF059669`); delivery `_mergedCard` cardBg wg `white@0.06/0.30` + border wg `white@0.85`; merge-pill inside `_dashOrderCard` wg → `white@0.16/0.32` + white border. `glass_theme.dart` `glassTint` wg → `Colors.white`, `glassAlpha` wg → `0.08` (pehle dark `0xFF152238` @0.52 = black tint; isi se dashboard cards black lagte thay).
- **Cashier Mark Paid visibility FIX (root cause):** `cashier_options.dart` `_key(ns)` ab **hamesha shared** `'cashier_options_v1'` return karta hai (pehle delivery `dlv_` / nashta `nsh_` alag keys — ek app mein ON karo to doosre app ko data nahi milta). Delivery + nashta printer_settings ka master "Mark Paid (Cashier)" toggle ab ON par **dono flags** (`markPaidOrders` + `markPaidDashboard`) set karta hai. **Gotcha:** purana `dlv_/nsh_cashier_options_v1` data orphan ho gaya — settings mein `Mark Paid (Cashier)` ko dobara ON + Save karna hoga, warna shared key empty rahegi.
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (baseline waisa), `flutter build apk --release` ok (**55.9MB**, 58578666 bytes) → copy `D:\pos\usman_hotel_app.apk`.
- **Gotcha:** delivery dashboard AnimatedSwitcher wrap ke baad bracket count +1 (`ListView` closing ke baad 1 extra `),`) — plus `_buildOrdersTabs() {` line galti se ud gaya tha `Widget _buildOrdersTabs() {` re-add kiya. Record-pattern switch expression (`final (a,b,c) = switch`) avoid kiya (saaf `switch` statement use kiya — safer).

## Task: Glassy Theme 1.2 refine — transparent/glassy dashboards (mode 3): transparent POS bg + white shiny borders, rider sheet transparent, filters icon, search icon removed — DONE (Sep 2026)
- **User:** "total glassy look, hr color dashboard sai remove kr do" ki AGAYI — mode 3 par **colors remove karo, sab transparent/glassy**: revenue card + 4 count cards ka color hatao (transparent frosted, white shiny border), due/reserved order card background transparent glassy + white shiny border + white text, dashboard background transparent taki POS photo dikhe, rider-assign sheet (heading bar/search/order pills) transparent glassy + white text, search bar ka prefix search icon REMOVE (sirf ✕ clear rahe), rider/user filter hides into ek Filter icon toggle. Classic/GlassDark/GlassLight untouched; sab change sirf `GlassTheme.isNewGlass` (mode 3) par.
- **`glass_theme.dart`:** `overlayColors` wg-branch ab **HALKA** (`0x4D03070F`/`0x590B1524`, ~30% alpha; pehle `0x8C`/`0x99` = 55-60%) — dono dashboards + order screens par POS photo zyada visible. `isNewGlass` hai to `isDark` bhi true rahta hai (dark palette branches mode 3 par auto-apply).
- **Delivery dashboard (`delivery/dashboard_screen.dart`):** `_card` (Revenue big stat) wg → `white@0.06/0.30` transparent + **white border a0.80** + neutral black shadow (color tint HATA); `_mini` (4 count cards) wg → transparent + white border a0.80 + neutral shadow (color glow hata); `_dashOrderCard` wg → bg `white@0.05/0.30` (amber/reserved color blend HATA) + white border `a0.60/0.90` (selected/custom colors sirf non-wg); `_RidersOrdersSheet` header + bottom bar wg → `white@0.08` transparent, order-card `white@0.06` + white border a0.65, search field `white@0.06` fill + **prefixIcon (Icons.search) HATAYA**; `_MergedSheet` header wg → transparent. Filters: naya `bool _filtersOpen` + `_buildOrdersTabs` mein dono `_filterDropdown` (All Riders/All Users) ab ek **Filters icon toggle** (`Icons.filter_alt`/`filter_alt_off`) ke peeche hide; `_dashSearchField` prefix icon bhi hata + wg fill transparent.
- **Nashta dashboard (`nashta/dashboard_screen.dart`):** `_card`/`_mini` wg transparent + white shiny border (same); `_dueOrderRow` wg → bg `white@0.05/0.30` + white border a0.65/0.9 (amber tint HATA, text pehle se white); `_RidersOrdersSheet` header/bottom-bar/order-card transparent + white border + search field transparent + prefix icon remove. (Nashta dashboard par rider/user filter dropdowns hain hi nahi — sirf delivery wala icon-toggle.)
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (baseline waisa), `flutter build apk --release` ok (**55.8MB**, 58562130 bytes) → `D:\pos\usman_hotel_app.apk`.
- **Gotcha:** removed prefixIcon ke saath `filled: true` bhi galti se hat gaya → re-add kiya (fillColor sirf `filled:true` par apply hota hai).

## Task: Glassy Theme 1.2 white → dark-surface revert (mode 3) + L→R sweep on order screens — DONE (Sep 2026)
- **User:** "total glassy look, hr color dashboard sai remove kr do" — naya **Glassy Theme 1.2** mode par saari surfaces frosted WHITE, saara accent/color text WHITE (colored labels sirf text rahen). Purane themes (Classic/Glass Dark/Light) untouched. Dono apps settings + order screens par bhi naya look, requeued press animation.
- **`glass_theme.dart`:** naya mode **`glassNew = 3`** (`GlassTheme.glassNew`, getter `isNewGlass` = `mode.value == 3`). **`GlassChip`** ab mode-3 aware — selected: white gradient fill (a0.85/0.65), white border (a0.95, w1.3), text white + black-soft-shadow, sweep alpha 0.9; unselected: white a0.30/0.16, white border a0.65, sweep 0.75. **`GlassFillChip`** (order screen rider pills) pehle se wg-ready tha (white translucent + white text + full-width lingering white sweep L→R — `_FillChipPainter` progress width).
- **Delivery dashboard (`delivery/dashboard_screen.dart`):** top-level palette ab mode-3-aware GETTERS (const se convert): `_accent` (wg→white, else `0xFF2563EB`), `_border` (wg→white a0.55), `_txtDark` (wg→white), `_txtDim` (wg→white70). **`_DashboardScreenState` static consts bhi static getters:** `_reserved` (wg→white, else `0xFF991B1B`) / `_reservedBg` / `_mergeBox` (wg→white, else `0xFF4C1D95`). `_mergedCard`/`_mergeBar` (ElevatedButton white-glass bg + white border + white fg)/`_customDateBtn`/`_dateSep`/merge-clear-pill sab wg-white. `_RidersOrdersSheet` order card Total wg→white (`0xFF059669` light); bottom good pills (Assign Rider/Mark Paid/Mark Due) wg branches (white bg + white border, non-wg old purple/green/amber). `_MergedSheet` member-card border `0xFFDDD6FE`→wg white a0.5/0.85. `_dashOrderCard` pehle se wg-aware.
- **Nashta dashboard (`nashta/dashboard_screen.dart`):** poora port — top `const Color _accent` → getters (wg-white), `_border/_txtDark/_txtDim` wg-aware; `_riderRow` wg (name white, glassy avatar, wg count pill white), `_card` (wg white-a bg + neutral shadow + white border), `_mini` (wg white value + white border), `_miniBtn` (white glass), `_buildDuePanel`/`_dueRiderBlock`/`_dueOrderRow` (wg white, markers/Totals white), `_RidersOrdersSheet` order-card Total wg→white + bottom Mark Due/Paid/Change-Rider buttons wg-white glass.
- **Settings — "Glassy Theme 1.2" tile dono apps mein:** `nashta/printer_settings_screen.dart` `_buildAppearanceCard` + **`delivery/printer_settings_screen.dart``** (jo pehle light-only static consts tha; ab `../glass_theme.dart` import + naya `_buildAppearanceCard()` sab se UPAR ListView mein) — dono mein `GlassTheme.glassNew` tile (`Icons.blur_on`, `0xFF38BDF8`, horizontal, sky gradient preview) Glass Dark/Light row ke neeche, Classic ke upar. Tile `ValueListenableBuilder<int>(GlassTheme.mode)` se live-update.
- **Order screens (rider filter pills):** delivery `_ordersRiderFilterBar` (GlassFillChip) pehle se wg-white tha — koi edit nahi. Nashta order_taker mein rider filter bar hai hi nahi.
- **Rider tap:** wiring confirm — `InkWell(onTap…) > GlassSweep > GlassPanel`; GlassSweep `Listener(HitTestBehavior.translucent)` raws events observe karta hai (tap arenas mein compete nahi karta) → rider card tap `_openRiderSheet` OK.
- **Gotchas (is task):** (1) static getter ko INSTANCE se access (`p._mergeBox`) error `instance_access_to_static_member` — `_DashboardScreenState._mergeBox` (class name) use karo. (2) Getters banane ke baad `const TextStyle(color: _accent)`/`const Icon(color: _accent)` compile TODTEn — non-const karo (nashta N:474/N:1188 fixes). (3) `_RidersOrdersSheet` children list mein `final wg` declaration ghalti se Wrap children mein aa gaya → build top par move kiya.
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (baseline waisa), `flutter build apk --release` ok (**55.9MB**) → copy `D:\pos\usman_hotel_app.apk`.

## Task: Glass dashboard round 2 — chip-style sweep on rider cards, count pills, due/reserved cards, stat cards + POS photo on order screens — DONE (Sep 2026)
- **User:** Filter chips wala glassy effect + left→right press-sweep animation ab **rider cards, count pills, dashboard blocks, due/reserved tab ke har order card, aur revenue/stat cards** par bhi; text visible (rider cards "white color" glassy). Glass theme mein **dashboard aur order screens dono** mein POS photo light blur ke saath visible.
- **`glass_theme.dart`:** naya **`GlassSweep`** StatefulWidget (GestureDetector translucent + `AnimatedScale 0.98/0.99` + AnimationController 780ms + `_SweepBandPainter` white band left→right, `radius` param, classic mode painting off) — koi chhiota surface wrapper. **`GlassBackdrop` blur 5 + light overlays (pichla task)** ka matlab **order_taker screens dono** (`GlassBackdrop()` default) ab bhi POS photo show karte hain — koi naya edit nahi.
- **Rider cards (dono dashboards `_riderRow`):** ab `GlassSweep(radius:12)` + `GlassPanel(tint: Colors.white, alpha dark 0.10 / light 0.55, white border 0.30/0.75)` — chips jaisa frosted white; naam darker accent (`lerp black 0.24`), sub-line dim; **count pill ab frosted white chip** (`white@0.18 dark / 0.65 light`, white border, soft shadow, accent-dark bold 16). Outer boxShadow neutral black.
- **Delivery `_dashOrderCard` (due/reserved):** `GlassSweep(radius:12, scale:0.99)` wrap — har order card par bhi press-sweep; bg/colors/sharp text pichla task se.
- **Stat cards (dono `_card`):** ab `GlassSweep(radius:16)` + glass mode bg = `alphaBlend(c@0.42 dark/0.58 light, white@0.06)` (frosted tinted, white text visible rehta); `_mini` bhi `GlassSweep(radius:14)` wrap. Classic mode waisa solid color.
- **Bracket gotcha:** rider pill banate waqt old pill ka `style: ... Colors.white)),` + extra `),` reh gaya tha → syntax error; cleanup + closing chain (GlassSweep = +1 level) carefully fix kiya. End of `_dashOrderCard` mein bhi GlassSweep wrap par 1 extra `),` add hua.
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (baseline), `flutter build apk --release` ok (**55.8MB**) → copy `D:\pos\usman_hotel_app.apk`.

## Task: Glass dashboard revamp — POS photo visible + glassy date chips + press-sweep + rider count + sharp due/reserved — DONE (Sep 2026)
- **User:** (1) Dono apps k dashboards mein **POS background photo show** ho, sirf **light blur** (zara sa). (2) **Date range bubbles/chips** ko glass effect do — white + transparent, text visible. (3) **Doosray bubbles (saare chips)** par press karne par glass animation left→right **slow motion**. (4) Riders & Orders section: rider **naam ka glow hatao**, color bubble se match karein (zara **darker**), **orders count bold + visible + bara** font. (5) **Due aur Reserved** tab orders: **text glow hatao**, colors **sharp/visible**, card background **halka + glassy** (background shadow ke saath).
- **`glass_theme.dart`:** `GlassBackdrop` default blur **12 → 5**; `overlayColors` dark `0xCC04070D/0xD90A1526` → **lite `0x8C050A18/0x990F1B33`**, light `0xB8/0xC2` → `0x8CFFFFFF/0x99E7F1FF` — ab POS photo blur-light visible rehti hai. **Naya `GlassChip`** StatefulWidget: frosted white-transparent unselected bubble (dark `white@0.20/0.10`, light `0.72/0.55`), selected = accent gradient fill + white text; press par `AnimatedScale 0.92` + **`_ChipSweepPainter`** (780ms slow white band left→right). Classic mode mein fabric blur 0 (backdrop normal).
- **Date chips (dono dashboards):** delivery + nashta `_buildDateFilter` ChoiceChip → `GlassChip(label, selected, accent: _accent, onTap)`.
- **Delivery order-tab chips (`_orderTabChip`):** ab `GlassChip` (`'$label ($count)'`) — saare tab bubbles par bhi press-sweep glass animation.
- **Riders (dono dashboards `_riderRow`):** naam Text glow shadows HATA — `Color.lerp(c, Colors.black, 0.24)!` (bubble ka darker shade); count pill ab **solid `c` fill + white w900 font 16** (pehle translucent + glow, ab bold/bara/visible) with `BoxShadow(c@0.5 blur 10)`.
- **Delivery `_dashOrderCard` (due/reserved tabs):** 6 `_glowText` usages → **sharp `Text`** (loc/addr/Total/date/pickUp/deliveryUser, w900, no shadows); `glow`/`glow2` ab theme-aware solid colors (dark: `0xFFF87171`/`0xFFFDBA74` reserved/due, light original). Card bg glass mode = `Color.alphaBlend(accent@0.05-0.08, white@0.10-dark/0.50-light)` — **halka glassy**; border accent alpha 0.65(dark)/0.9(light); boxShadow glass = soft black (dark 0.38 blur16 / light 0.14 blur16 offset 0,6), classic = pehle wala glow shadow. Items panel bg theme-aware (`white@0.10` dark / `0.7` light) + amber border; item/notes text colors theme-aware.
- **Nashta `_dueOrderRow`:** glass mode bg blend (`B45309@0.06/0.08` + white glass), socha shadow + `FBBF24@0.55/0.85` border; Total glow shadows HATA — solid `0xFFFBBF24` (dark) / `0xFFB45309` (light). **Nashta dashboard mein Reserved tab nahi** — sirf Due panel; reserved wala delivery mein hai.
- **Kya rehta hai (pehle se):** delivery/nashta `_card`/`_mini` big-stat glow aur merged-group card glow (`_edgeCard`/`_mergedCard`) waisay hain (user ne nahi bola); classic theme untouched (sab old look → glass-only changes).
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (baseline waisa hi), `flutter build apk --release` ok (**55.8MB**) → `D:\pos\usman_hotel_app.apk`.
- **Gotcha:** GlassChip mein nested BackdropFilter (chip andar GlassPanel) — 7 chips OK, performance fine; AnimatedDefaultTextStyle avoid kiya (direct rebuild); sweep painter RRect+gradient shader se draw karta hai.

## Task: Printer Settings — naya "Options" (Cashier permissions) + dark glass dashboard speed + glow boost — DONE (Sep 2026)
- **User:** (1) dono apps (delivery + nashta embedded) ke Bluetooth/Printer settings mein naya **Options** section — manager decide kare cashier ko **Mark Paid** aur **Return to Active** buttons dikhein ya na + kin screens par (Orders Screen / Dashboard). (2) **Dark glass dashboard** open hone mein time lagta hai → speed up. (3) Dark glass mein **stats / orders count / due-reserved order text** glow/sharp/colorful banao.
- **Naya shared file `usman_hotel_app/lib/embedded/cashier_options.dart`:** `CashierOptions` (markPaidOrders, markPaidDashboard, returnActiveOrders, returnActiveDashboard; sab default false; copyWith/toJson/fromJson) + `loadCashierOptions(ns)`/`saveCashierOptions(ns, o)` key `${ns}_cashier_options_v1`. Namespaces: delivery = `'dlv'`, nashta = `'nsh'`.
- **Printer settings (dono screens):** `_buildOptionsCard()` + `_optChip()` helper — har action ka master `_toggle('Mark Paid (Cashier)' / 'Return to Active (Cashier)')` + do chips Orders Screen / Dashboard. Nashta save/load `_cashierOpt` (ns 'nsh'); delivery same (ns 'dlv'). Delivery printer settings light-only static consts raha.
- **Delivery order_taker gates:** `_cashierOpt` field + `_loadCashierOpts()` (initState + Printer settings pop par `.then` mein `_loadBtOverrides` ke saath) + helpers `_showMarkPaidOrders()` (`_isManager() || (_isCashier() && markPaidOrders)`) aur `_showReturnActiveOrders()` (`_isManager() || (_isCashier() ? returnActiveOrders : true)` — non-cashier hamesha allowed). Reserved-tab Paid: `!_isCashier() || markPaidOrders`; delivery/non-delivery Paid blocks `_showMarkPaidOrders()` par; **Due chip manager-only raha** (split kiya); Return to Active par `_showReturnActiveOrders()`; merged-group Mark Paid (`_bulkMarkPaidList`) dono jagah option-aware; bulk bar Mark Paid `!_isCashier() || markPaidOrders`.
- **Nashta order_taker gates:** `_isCashier()` bhi add kiya (pehle tha hi nahi); same helpers + wiring (ns 'nsh'). Delivery Paid block jo pehle manager-only tha split hua; non-delivery Paid gate `_showMarkPaidOrders()`; Due waisa.
- **Dashboards:** delivery dashboard mein `_cashierOpt` + getters `_dashShowMarkPaid`/`_dashShowReturnActive` + load in initState; `_dashOrderCard` reserved tab actions — manager full row, else if `_dashShowMarkPaid` sirf Print+Mark Paid; non-reserved Mark Paid/Return to Active option-aware; merged card + `_MergedSheet` + `_RidersOrdersSheet` (naya `showMarkPaid` param) sab option-aware. **Nashta dashboard:** `loadCashierOptions('nsh')` initState (naya initState add kiya), `_dashShowMarkPaid`, due-row Mark Paid gated, `_RidersOrdersSheet` naya `showMarkPaid` param bulk Mark Paid gate.
- **Speed (Task 2):** delivery `_refreshOrders` ab `Future.wait` — dono source grabs (`?source=bbq-delivery-app` + `?source=nashta-app`) aur `/pos/products` **parallel** (pehle serial 3× round trip); nashta `_refreshOrders` bhi orders + products parallel (`Future.wait`).
- **Glow boost (Task 3):** dono dashboards + dono order_taker files ke `_glowText` ab theme-aware — dark glass mein double shadow (alpha 0.85 blur 15-16 + alpha 0.45 blur 28-30), light mein waisa single (alpha 0.6 blur 8-9). Delivery + nashta `_card` (big stat) value white glow + `_mini` value color glow; `_riderRow` naam + count pill glow; nashta due-row Total amber glow. Nashta dashboard mein `_glowText` helper naya (copy).
- **GOTCHAS:** (1) nashta `_riderRow` naam edit mein extra closing paren `]))))` uda tha → syntax error `DashboardScreen 596` — trailing parens ko hamesha count karo / ambiguity ho to basic `_glowText` use karo. (2) `_glowText` helper ek baar ghoom ke file se gayab ho gaya (warning `unused_element` → baad mein `undefined_method` errors) — re-add method fix.
- **Verify:** `flutter analyze --no-pub` **0 errors, 44 issues** (pehle wale baseline count waisa hi), `flutter build apk --release` ok (55.7MB) → copy `D:\pos\usman_hotel_app.apk`.

## Task: Classic theme PURE WHITE + refresh/sync/full buttons hataye (sirf search + back) — DONE (Sep 2026)
- **User:** Classic (Default) theme mein dashboards + orders screens (dono apps) + order cards sab **pure white** background chahiye; aur top bar se **Refresh / Sync / Full refresh buttons + sync icon (cloud)** remove — "app khud fast sync ho jata hai", bas **search toggle + back (close) icon** rehna chahiye.
- **`glass_theme.dart`:** naye getters — `screenBg` (classic → `Colors.white`, dark → `0xFF0A1020`, light → `0xFFF8FAFC`), `insetBg` (classic → white, dark → `black@0.22`, light → `0xFFF8FAFC`), `barBg` (classic → white, dark → `0xFF101A2E`). `GlassBackdrop` classic ab `ColoredBox(color: Colors.white)` (pehle `0xFFF8FAFC`).
- **Replacements:** delivery dashboard sheets 654/2240 + nashta dashboard sheet 250 → `GlassTheme.screenBg`; delivery/nashta `_orderDetail` panel bg → `GlassTheme.insetBg`; orders-screen search/form fields (`fillColor: const Color(0xFFF8FAFC)`) → `GlassTheme.fieldFill` (classic = white, glass = translucent); nashta printer_settings `bg` getter light branch → classic par `Colors.white`. Delivery dashboard bottom bars pehle se `isDark ? 0xFF101A2E : Colors.white` (classic white).
- **Buttons hataye (orders screens ke top bars):** delivery `_ordersScreen` — `_syncIndicator()` + 3 `_rsBtn` (`Refresh`/`Sync`/`Full`) + delivery main screen bottom-right `_syncIndicator()`; nashta orders header `_syncIndicator()`. Ab delivery top bar = `Delivery` title + calendar + search toggle + select + close; nashta = `Nashta` + calendar + search + select + close. Auto-sync waisa chalta hai (silent refresh).
- **Dead code remove kiya:** delivery se `refreshBusy` field, `_manualRefreshOrders`, `_manualFullRefresh`, `_rsBtn`, `_syncIndicator` (dono files)! `lastSyncOk` field + its setters (`_markSyncOk/_markSyncFailed`) ab write-only hain (kisi read nahi hota) — `unused_element`/warning acceptable.
- **GOTCHA (khud ka):** deletion ke waqt wrong `oldString` se `_openQuickDueScreen()` signature accidental replace ho gaya (duplicate `_syncIndicator` + clobbered method). Recovery: context read karke badla sau lines mehnt se — hamesha method delete se pehle poori method body + surrounding context read karo aur `oldString` mein next method ka pehla line bhi include karo.
- **Verify:** `flutter analyze --no-pub` **0 errors** (44 pre-existing infos/warnings — count same), `flutter build apk --release` ok (55.7MB) → `D:\pos\usman_hotel_app.apk`. `_setBt` + `unnecessary_underscores` infos unchanged.

## Task: Classic (old default) theme wapis + 3-way theme picker + delivery dashboard rider bar order qty — DONE (Sep 2026)
- **`usman_hotel_app/lib/embedded/glass_theme.dart`:** ab **teen modes** — `GlassTheme.classic` (0, purana flat light look), `glassDark` (1), `glassLight` (2). Nayi `ValueNotifier<int> mode` (pref key `uh_glass_theme_mode`; legacy bool key `uh_glass_theme_dark` auto-migrate); `dark` notifier back-compat ke liye sync rehta hai (`setDark(true/false)` → `setMode(1/2)`). Naye `setMode(int)`, `isClassic`, `isGlass`, `classicSurface`. `GlassBackdrop` classic mein `ColoredBox(0xFFF8FAFC)` (koi photo/blur nahi); `GlassPanel` classic mein **opaque** white card (tint ho to `Color.alphaBlend(tint@0.10, white)`) + `0xFFE2E8F0` border + halka shadow (BackdropFilter nahi); `chipBg/chipBorder/fieldFill/glassBorder` classic par opaque white/light-gray. Baqi saare palette getters `isDark` false hone par light values dete hain.
- **Picker (`nashta/printer_settings_screen.dart`):** `_themeTile` ab `{required int value,...}` + `horizontal` flag; Appearance card mein Glass Dark + Glass Light row ke neeche full-width **"Classic (Default)"** tile (`Icons.layers_outlined`, `0xFF059669`). `ValueListenableBuilder<int>(GlassTheme.mode)`.
- **Listeners:** chaaron screens (delivery/nashta dashboard + order_taker) ka `ValueListenableBuilder<bool>(GlassTheme.dark)` → `<int>(GlassTheme.mode)` (builder param `dark`/`isDark` unused thay).
- **Startup persistence FIX:** `usman_hotel_app/lib/main.dart` (asli entry) mein `await GlassTheme.load();` add — pehle sirf `embedded/nashta/main.dart` mein tha jo entry nahi hai, is liye theme restart par default ho jati thi.
- **Delivery dashboard `_riderRow`:** order qty wapas — sub-line `"${orders.length} active • Delivery Charges: X PKR"` + right side coloured **count badge** (pill, `orders.length`) chevron se pehle.
- **GOTCHA (bara):** PowerShell `Get-Content -Raw | Set-Content -NoNewline` loop se **BOM wali UTF-8 file** ANSI mein likh kar tabah ho jati hai (saare Urdu/emoji `?`). `nashta/order_taker_screen.dart` aise corrupt hua → `flutter analyze` ne "Target of URI doesn't exist: order_taker_screen.dart" + `sOf/numOf undefined` diya. **Recovery:** `git show HEAD:file` → `base_real.dart`; usi ko non-ASCII→`?` karke `base_norm.dart`; current mangled ko `ours.dart`; `git merge-file` (default style, `|||||||` markers NAHI); conflicts (theirs side) resolve. **Aage:** file edits hamesha `edit`/`write` tool ya .NET `[System.IO.File]::WriteAllText($f,$t,(New-Object System.Text.UTF8Encoding($false)))` se karo — `Set-Content`/`Out-File` se kabhi nahi (ye UTF-8 bytes ko console/OEM encoding se decode kar ke likhte hain). Native command ka output capture karke file mein likhna bhi mojibake deta hai (cp437 box-drawing chars) — `git merge-file <target> <base> <other>` in-place likhne ko prefer karo.
- **Verify:** `flutter analyze --no-pub` 0 errors (44 pre-existing infos), `flutter build apk --release` ok (55.7MB) → copy `D:\pos\usman_hotel_app.apk`. (User ne pehle "APK na banao, device par run karo" kaha tha lekin koi Android device/emulator connected na hone par APK build karne ko kaha.)

## Task: iOS-style glass UI rollout (nashta + delivery orders screens + BOTH dashboards) + dark/light theme + theme picker — DONE (Sep 2026)
- **Shared file `usman_hotel_app/lib/embedded/glass_theme.dart`:** `GlassTheme` (ValueNotifier<bool> `dark` + `SharedPreferences` key `uh_glass_theme_dark`, `load()/setDark()/isDark`, palette getters: `bgColors/overlayColors/blobA-C/blobAlpha/glassTint/glassAlpha/glassBorder/glassSheenTop/Bottom/textPrimary/Secondary/Dim/chipBg/chipBorder/fieldFill/divider/shadow`), `GlassBackdrop({image='assets/img/bbq_bg.jpg', blur=12})` (POS photo full-bleed + blur + theme `overlayColors` tint; `image:null` par gradient+3 blobs), `GlassPanel` (ClipRRect + `BackdropFilter(ImageFilter.blur)` + translucent gradient fill + 1.2px specular border + soft shadow + inner sheen; params radius/padding/margin/width/height/blur/alpha/tint/borderColor/shadow/sheen/strong), `GlassCircle`, `GlassBubbleButton` (coloured frosted chip: press par expanding white bubble + coloured glow + scale 0.90).
- **Nashta orders screen (`nashta/order_taker_screen.dart`):** `_ordersScreen` = `ValueListenableBuilder(GlassTheme.dark)` + `Stack(GlassBackdrop, SafeArea(...))`; local `_glass(...)` helper; top bar/views/sub-tabs/search sab frosted `_glass` panels. `_orderCard` = status-glow Container + `GlassPanel(borderColor: sel?blue:glow)`; delivery title `0xFF93C5FD`(dark)/`0xFF1D4ED8`(light), customer `textPrimary`; loc/subtitle/order# theme. `_orderDetail`/`_bulkBar` theme-aware. `_actChip` = `GlassBubbleButton`. Search bar ab `ordersSearchOpen` flag se toggle (`Icons.search`/`search_off` top bar mein date icon ke paas).
- **Delivery orders screen (`delivery/order_taker_screen.dart`):** same rollout ported — `import '../glass_theme.dart'`, `_glass` helper, `_ordersScreen` glass (header mein title "Delivery" 0xFF1D4ED8 + `_rsBtn` chips theme-aware, views/sub-tabs `GlassTheme.chipBg/chipBorder`, search `ordersSearchOpen` toggle 0xFF2563EB, `_bulkBar` glass). `_orderCard` = glow DecoratedBox + `GlassPanel(alpha .44)`; addr glow `0xFF93C5FD`(dark)/`0xFF1D4ED8`; loc/subtitle/order#/select icon theme. `_orderDetail` bg `black@.22`(dark)/`0xFFF8FAFC`, item/text colors theme. `_mergedOrdersCard`/`_mergedMemberRow` violet bg → dark translucent + `0xFFC4B5FD` text. `_ordersRiderFilterBar` chips theme. **NOTE: delivery app pehle se `returnToActive` + chip rakhta hai; nashta mein bhi add kiya tha (pichla task).**
- **Nashta dashboard (`nashta/dashboard_screen.dart`):** top-level `_border/_txtDark/_txtDim` const → **getters** (`GlassTheme.divider/textPrimary/textDim`) + ~20 `const` usages non-const; `build` `ValueListenableBuilder` + transparent Scaffold + `Stack(GlassBackdrop)` + custom glass header (Dashboard + greeting + close); `_buildDateFilter`/`_buildRidersPanel`/`_buildDuePanel`/`_mini` ab `GlassPanel`; `_riderRow` = coloured-glow DecoratedBox + GlassPanel(tint c); `_dueOrderRow` amber bg theme-aware (`FCD34D` dark text); date chips `GlassTheme.chipBg`. `_RidersOrdersSheet` (bottom sheet) bhi theme-aware (header bg `0xFF101A2E` dark, Card `white@.08`, search `fieldFill`).
- **Delivery dashboard (`delivery/dashboard_screen.dart`):** same — consts → getters (36 `const` keywords auto-stripped via balanced-paren script), `build` glass Stack + header, `_buildOrdersTabs`/`_buildDateFilter`/`_buildRidersPanel`/`_mini` = GlassPanel, `_riderRow` glow glass, `_orderTabChip`/`_filterDropdown`/`_dashSearchField` theme, `_dashOrderCard` bg/dark tinted, `_mergedCard` violet bg theme, `_MergedSheet` + `_RidersOrdersSheet` theme-aware, bottom bars `0xFF101A2E` dark.
- **Printer settings (`nashta/printer_settings_screen.dart`):** const colours → getters, `ValueListenableBuilder` wrap, `_buildAppearanceCard()` + `_themeTile()` (Glass Dark / Glass Light picker). `nashta/main.dart`: `await GlassTheme.load();` before runApp.
- **Verify:** `flutter analyze --no-pub` **0 errors** (44 issues = pre-existing infos + `_setBt` warning), `flutter build apk --release` ok (55.7MB) → copy `D:\pos\usman_hotel_app.apk`. Default theme **dark**.
- **Gotchas:** `unnecessary_underscores` → `(_, _)` use karo (nahi `(_, __)`); `const TextStyle` mein `.withValues()` allowed nahi; consts ko getters banane se enclosing `const` widgets todte hain (bulk removal ke liye balanced-paren PowerShell script use ki); nested `BackdropFilter` (glass chip inside glass card) performance par nazar rakho.


## Task: nashta takeaway/table customer name restore — DONE (Sep 2026)
- **`usman_hotel_app/lib/embedded/nashta/order_taker_screen.dart` `_orderCard`:** `cust = sOf(o['customerName'])` + `isDelivery = type.toLowerCase()=='delivery'` add. TOP text ab: **delivery → glowing address/loc (waisa hi)**; **Takeaway/Table → customer name** bold 13.5 w900 `0xFF0F172A` (fallback addr/loc agar cust khali). Location row + type pill + subtitle (time · day, date · taker) waisa. `flutter analyze` 0 errors (45 pre-existing infos), build ok (55.5MB) → `D:\pos\usman_hotel_app.apk`.
- **NOTE (reverted):** Ek alag app `D:\pos\usman_hotel_new` (glass notification screen) user request par bana kar **phir REMOVE kar diya gaya** — user ne bhi bola "notification wali screen nahi chahiye thi". Ab `D:\pos\usman_hotel_new` + `D:\pos\UsmanHotel-New.apk` **maujood NAHI hain**. User ka asli intent: `usman_hotel_app` ke embedded delivery + nashta ke **dashboard screen aur orders screen** ko reference photo jaisi **glass/frosted (iOS notification-panel style backdrop blur)** look dena — notification screen alag se nahi banana. (Ye UI kaam abhi pending hai, confirm karke karna.)
- **Gotcha:** `flutter build` (naya project pehli dafa) 8-15 min le sakta hai; Gradle 9.3.1 cache mein tha.

## Task: usman_hotel_app — delivery dashboard rider charges + rider-assign card redesign + date/day on order cards (dono apps) — DONE (Sep 2026)
- **Scope:** `D:\pos\usman_hotel_app\lib\embedded\delivery\dashboard_screen.dart` + `delivery/order_taker_screen.dart` + `nashta/order_taker_screen.dart`. `flutter analyze --no-pub` NO new issues (sirf pre-existing infos + `_setBt` warnings), `flutter build apk --release` ok (55.5MB) → `build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\usman_hotel_app.apk`.
- **Dashboard `_riderRow` (delivery):** "N active • Nashta fees: X PKR" sub-line + right count badge HATA diya. Ab sirf loader: rider naam (bold glow) + ek line `Delivery Charges: X PKR` — naya helper `_deliveryCharges(rider)` = sum `deliveryFee` over `_inRange` non-cancelled delivery orders **jinki source ≠ nashta-app** (matlab delivery app ke apne orders) assigned to rider. Purana `_riderCharges` (nashta-fee) remove kar diya (ab unused).
- **Rider-assign sheet (`_RidersOrdersSheet` order cards):** redesign — **address TOP par bold blue glow** (`0xFF2563EB` + Shadow blur 7), us ke neeche **location bold** (`_txtDark` w800), phir **`Total: X PKR` bold green**, aur sab se **neeche Order #** dim. Total formatted via local `totS` (round v/s 2-decimals).
- **Order cards date+day (dono apps):** delivery + nashta `order_taker_screen.dart` mein naye helpers `dayName(iso,{utcOffset})` (Mon..Sun) + `shortDate(iso,{utcOffset})` (dd/MM/yyyy) added (delivery `esc.applyTimeZone`, nashta `raw.add(hours)`), aur subtitle ab `…· time · Day, dd/MM/yyyy` dikhata hai (e.g. `Delivery · 3:45 PM · Mon, 14/09/2026`). Delivery card mein `tzOff = esc.timeZoneOffsetFor(...)` variable nikala.
- **Note:** nashta dashboard `_riderRow`/rider-sheet touch NAHI kiye (user ne sirf delivery ke liye kaha tha; date/day dono apps mein hai).

## Task: usman_hotel_app — nashta rider-sheet redesign + glowing address on order cards + collapsible views/tabs + date-filter icon (dono apps) — DONE (Sep 2026)
- **Scope:** `D:\pos\usman_hotel_app\lib\embedded\delivery\` + `lib\embedded\nashta\` (`dashboard_screen.dart` + `order_taker_screen.dart`). `flutter analyze --no-pub` 0 errors (45 pre-existing infos + `_setBt` warnings), `flutter build apk --release` ok (55.5MB) → `build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\usman_hotel_app.apk`.
- **Nashta dashboard rider-assign sheet (`nashta/dashboard_screen.dart`):** REDESIGN kiya mirror delivery — order card ab: **address TOP bold glow** (`_accent` `0xFF2563EB` + Shadow blur 7), neeche **location bold** (`_txtDark` w800), phir **`Total: X PKR` bold green** (`0xFF059669`), sab se neeche **Order #** dim. Locals `tot`/`totS` added (total round v/s 2-decimals via `_fmtNum`). Tab-text wala `Text(#$num)` bottom dim rakha.
- **Order cards → customer name HATA, glowing address + location top (dono apps):** `order_taker_screen.dart` `_orderCard` mein `final cust = sOf(o['customerName'])` declare karna BAND + purane lower loc/addr block remove. TOP Row ab: **`addr.isNotEmpty ? addr : loc`** bold w900 13.5 glow `0xFF1D4ED8` + Shadow `alpha 0.5` blur 7 (delivery reservedTab par `0xFF991B1B`) + delivery pill (`_deliveryPill()` delivery app / `_typePill(type)` nashta). Jab dono (addr+loc) non-empty ho to loc alag row: `Icons.location_on` size 13 `0xFF2563EB` + loc w800 12.5 `0xFF1E293B` (maxLines 2, ellipsis). Subtitle (time · day, date · taker) waisa.
- **Orders screen → views/tabs collapse (dono apps):** naya `bool ordersTabsOpen = false` field. Views row button tap = toggle; sub-tab select par auto-close (`ordersTabsOpen = false`). Selected view label ab `'$v · $currentSubLabel'` (currentSubLabel = subTabs.firstWhere(ordersSubTab)[1]) + expand_more/expand_less chevron icon. subTabs `Container` wrapped in `if (ordersTabsOpen)` (vertical space bachta hai — orders puri dikhti hain). Delivery single-view hai (`views = ['Delivery']`, onTap sirf toggle; `ordersView` field still para read-only); **nashta 3-views** (Delivery/Takeaway/Table) — subTabs ab helper `List<List<String>> _subTabsFor(String view)` (Delivery→active/assigned/paid/due/cancelled, Takeaway→payLater/due/paid/cancelled, Table→active/due/paid/cancelled); naya view select par `ordersView=v; ordersSubTab=_subTabsFor(v).first[0]; ordersTabsOpen=true`.
- **Date filter → icon + sheet (dono apps):** inline `_ordersDateFilterBar()` row HATA diya. Top action bar (select icon ke paas, same size) mein naya `IconButton(Icons.calendar_month, color 0xFF0EA5E9, tooltip 'Date Filter: <range>')` → `_showOrdersDateSheet()` (modal bottom sheet: title, `Wrap` of `_ordersRangeChips(onChanged: ...)`, custom From/To pickers + Reset vibhi square jab `ordersDateRange == 'custom'`). `_ordersRangeChips()` ab `{VoidCallback? onChanged}` param leta hai aur `onSelected` mein `setState(...); onChanged?.call();` karta hai.
- **Gotchas:** nashta order-card glow `TextStyle` ke andar `.withValues(alpha: 0.5)` **NOI constant** — `const TextStyle` compile error `const_eval_method_invocation` deta hai, is liye non-const (delivery file bhi non-const hai). `_showOrdersDateSheet` sheet ke andar `StatefulBuilder` + `setSheet` se chip changes live reflect hote hain; custom-date `setState` ke baad `refresh()` (close sheet).

## Task: order_taker_app orders screen — search + counter-type filter + button-only press-fade — DONE (Sep 2026)
- **Scope:** `D:\pos\order_taker_app` (`OrderTakerScreen`, `lib/order_taker_screen.dart`). `flutter analyze --no-pub` clean (0 issues), `flutter build apk --release` ok (54.4MB) → `build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\order_taker_app.apk`. Gradle/Kotlin/AGP upgrade warnings sirf deprecation notices — koi action nahi.
- **Orders search bar:** `_ordersScreen` mein sub-tabs/date/user filters ke neeche `_ordersSearchField()` added — `ordersSearchCtrl`/`ordersSearch` state. **Shudh filter** `filteredVisibleOrders` mein: customer name, Order # (orderNumber || id), aur total amount (`_fmtNum`) match karta hai (lowercase contains). Clear ✕ suffix button. `dispose()` mein controller dispose.
- **Counter-type filter har tab par:** naya `ordersTypeFilter` state + `_ordersTypeFilterBar()` — har orders tab/search bhi `ordersTypeFilter` show karta hai: chips `All Types` / `🛍️ Take Away` / `🍽️ Dine-In` (labels/colors `_typeChipLabel`/`_typeChipColor`). Nayi getter `ordersForCurrentTab`: filter khali → normal view logic (`ordersView == 'Takeaway' ? takeaway… : table…`); filter set → DONO lists merge (takeaway-only/table-only roles respect karte hue: `isTakeawayOnly`/`isTableOnly` cross-type block) aur `orderType == filter` match. `visibleOrders`/`orderUsersForCurrentTab` ab `ordersForCurrentTab` se.
- **Card action buttons → per-button press-fade (block STAYS):** `_orderCard` se POORA `_PressBlock` wrap HATA diya (card ab plain `Container`) — ab button press par poora block fade nahi hota. Naya `_PressFx` StatefulWidget (Listener based): press = AnimatedScale 0.92 + AnimatedOpacity 0.62 + tint overlay (`pressTint` color 0.22 alpha, `pressRadius` shape), release par `_reset` 110ms, `onTap` diya ho to single-fire with 350ms double-tap guard (`_lastTap`), `disabled` true par visual off. Har action button par apply: `_actionChip` + `_editIconChip` ab `_PressFx(onTap: …, pressTint: fg)` (visual container, inner GestureDetector remove — single fire), served-section buttons (Attach Payment Photo `0xFFB45309` r8, Mark Paid `0xFF059669` r8 `disabled:_busy`, Push to Farhan Owner `0xFF7C3AED` r8, Print `0xFF334155` r100, Add More Items `0xFF059669` r100), Delete Order `0xFFE11D48` r8 `disabled:_busy`, Retake chip `0xFF1E293B`. Bulk-bar `_bulkBtn` waisa `_PressBlock` (whole button fade) raha.
- **Takeaway/dine-in cross-type action correctness:** `_orderCard` mein naya `cardTab` (order ke `orderType` par): takeaway order → `takeawayTab ?? (ordersSubTab == 'new'||'served' ? 'pay_later' : ordersSubTab)`; dine-in order → null (hamesha table-style actions). Takeaway action branch `if (cardTab != null)` aur checks `cardTab == …` par; normal branch `if (cardTab == null)`. Fake tab switch se takeaway order ke sahi buttons (Mark Due/Paid/Cancel vs Delete order vs Mark Paid) render hote hain.

## Task: usman_hotel_app — glass icon action buttons (press-fade) + cross-app dashboards — DONE (Sep 2026)
- **Scope:** `D:\pos\usman_hotel_app` embedded delivery (`lib/embedded/delivery/`) + nashta (`lib/embedded/nashta/`). `flutter analyze --no-pub` 0 errors (sirf pre-existing infos/_setBt warnings), `flutter build apk --release` ok (55.4MB) → `build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\usman_hotel_app.apk`.
- **Orders screen action buttons → GLASS ICON chips:** dono apps mein `_orderDetail` action Wrap ab `_actChip(icon, label, color, onTap)` use karta hai — glossy gradient pill (top-left white-lerp → bottom-right dark-lerp), white 1.2 inner border, colored glow shadow, 16px Material icon + 8.5px bold label, warped in `_PressFx` (press = scale 0.92 + fade 0.65, 80ms → double-click/duplicate prevention). Icons: Print→`Icons.print`, Mark Paid→`check_circle_outline`, Due→`schedule`, Assign/Change→`delivery_dining`, Reserve→`lock_outline`/`lock_open_outlined`, Edit→`edit_outlined`, Cancel→`cancel_outlined`, Delete→`delete_outline`. Bulk bar (`Select All/Assign/Mark Paid/Reserve/Due/Delete`) waisa `_actBtn` (text) raha. Delivery ka `_actBtn` pehle se `_PressFx` tha; **nashta ka `_actBtn` bhi `_PressFx`+`_shadeDown` par laa diya** + naya `_PressFx` class file end par (bina `onTap` param — inner buttons khud fire karte hain, taake double-fire na ho).
- **Nashta cart "Create Delivery Order"** button ab `_PressFx` main wrapped (bg pressed → `0xFF047857`), same for "Update Order" edit mode. Duplicate protection pehle se `createOrder` ke `_busy` guard se thi; fade ab visual + extra safety hai.
- **Delivery dashboard (`delivery/dashboard_screen.dart`):**
  - `_all` getter ab `source` IN (`bbq-delivery-app`, `nashta-app`) — Total Orders / Revenue / Pending / Rider Assigned / Delivered sab nashta delivery orders bhi include karte hain (Due/Reserved tabs pehle se `_allDelivery` = all sources use karte thay; ab stats consistent).
  - `_revenueOf` = `max(0, total − deliveryFee − extras)` (extras = items jinki category mein 'extra'/'addon') — Delivery charges + extras category amount revenue se EXCLUDE. `_extrasAmountOf`/`_itemCategory` `_prodCats` (`/pos/products` categories, keyed id:/name:) se.
  - Rider boxes (`_riderRow`) → **alag highlighted box**: margin + tinted bg (accent `0xFF2563EB`, Unassigned amber `0xFFB45309`) + colored border + glow shadow, naam BOLD + glow shadow in c, sub-line `"N active · Nashta fees: X PKR"`. `_riderCharges(rider)` = sum of `deliveryFee` sirf **nashta-app** delivery orders (non-cancelled, in range) par rider assigned.
  - `_card`/`_mini` stat block HIGHLIGHT kiya: colored glow BoxShadow + white border (`_card`), colored border + glow shadow (`_mini`).
- **Nashta dashboard (`nashta/dashboard_screen.dart`):** same — `_prodCats` field + `_refreshOrders` ab `/pos/products` bhi fetch karta hai; `_itemCategory`/`_extrasAmountOf`/`_revenueOf`/`_riderCharges` (yahan source check zaroori nahi — `_inRange` already nashta only); `totalRev()` ab `_revenueOf`; `_riderRow` highlighted boxes + `"N active · Delivery fees: X PKR"`; `_card`/`_mini` glow. `takeawayPaid/Later` + `tablePaid/Active` stats unchanged.
- **Note:** revenue MIN subtract karta hai `deliveryFee` kyunki dono apps `total = subtotal + deliveryFee` bana kar save karte hain (total mein fee included hai).

## Task: order_taker_app new icon + timezone + light order cards — DONE (Sep 2026)
- **Icon:** New logo source `C:\Users\usman\Downloads\ChatGPT Image Sep 13, 2026, 07_34_24 PM.png` (1254×1254, no alpha, Format24bppRgb) → `order_taker_app\assets\img\logo.png`. Saare launcher icons regenerated (System.Drawing HighQualityBicubic downscale): `mipmap-{48,72,96,144,192}/ic_launcher.png` + `ic_launcher_round.png`; adaptive foregrounds `drawable-{108,162,216,324,432}/ic_launcher_foreground.png`; `values/colors.xml` (`ic_launcher_background` `#38BDF8`); `mipmap-anydpi-v26/ic_launcher.xml` + `ic_launcher_round.xml` (16% inset, same pattern as usman_hotel_app). "era"/BBQ branding HATA diya.
- **Login screen (`login_screen.dart`):** FULL rewrite — white background (`Colors.white`), centered new logo in rounded 108px container (green ring + glow), "USMAN HOTEL / Order Taker App" titles, clean `SIGN IN` card (max 400px), green `0xFF059669` button/login accents, `allowedRoles` check retained, red error box. No more bbq_bg.jpg/sunrise painter/ShaderMask gradient.
- **Splash/Gate (`main.dart`):** dono phi logo 120px rounded white container (green ring + glow) + `0xFF059669` spinner (pehle `0xFFF5C542`). SplashScreen + GateScreen dono updated.
- **Header (`dashboard_screen.dart`):** `_buildHeader` logo ab 36px rounded container (green ring, white bg) username ke paas — professional.
- **Time zone (Karachi/Islamabad/Lahore = UTC+5):** `escpos.dart` `timeZoneOffsetFor(String? region)` (empty→5, karachi/islamabad/lahore/pakistan→5, else 0) + `applyTimeZone(DateTime, int)` (iso/utc safe). `formatDate` ab `{int utcOffset = 5}`; escpos receipt + `bmp_receipt.dart` dateText dono timezone offset pass karte hain. `printer_settings_screen.dart`: `_receiptTimeZone` (default 'Karachi (UTC+5)'), prefs key `ot_timeZone`, settings map mein `'receiptTimeZone'`, `_buildTimeRegionCard()` (🕐 Time & Date Region dropdown, Urdu hint) inserted after `_buildTokenCard()`. Printer sheet (`order_taker_screen.dart`) Basic section mein bhi same "Time Region" dropdown (`_setBt('receiptTimeZone', ...)` pattern, `btSettings['receiptTimeZone']`).
- **Order cards (`_orderCard`, ~line 4890):** DARK (`0xFF0F172A`) → LIGHT delivery-app style: white card, rounded 14, status-colored border+glow (`glow`: paid `0xFF059669`, cancelled `0xFFDC2626`, served `0xFF0D9488`, else `0xFF2563EB`), customer name BOLD top in glow color + `_orderTypePill` (TAKE AWAY 🛍️ `0xFFB45309` / DINE-IN 🍽️ `0xFF059669`), subtitle line = `dateTime12(createdAt, utcOffset: _tzOffset())` + taker name, right column = total bold + status chip (PAID green/PENDING SYNC amber/CANCELLED red/small status pill), bottom row = ⏱ elapsed timer (left, state color) + "Tap for details"/"Hide details" + rotate ▼, aur sab se neeche dim `Order #<num>` row (receipt icon, dim `0xFF64748B`). `selectMode` checkbox :: new `_orderTypePill` helper method added before `_orderCard`. Expanded items/actions section (dark `0xFF020617` panel) WASAI raha.
- **Time helpers:** `time12`/`dateTime12` ab `{int utcOffset = 5}` (default 5 = Pakistan); card mein `_tzOffset()` (`timeZoneOffsetFor(btSettings['receiptTimeZone'])`) se; `paymentRequestedAt` date + cancelled-at time bhi `tz` pass karte hain (default-5 safe). `createdMs` unused ho gaya to remove kiya.
- **Verify:** `flutter analyze --no-pub` NO issues (0). `flutter build apk --release` ok (54.4MB) → `D:\pos\order_taker_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\order_taker_app.apk` (54.4MB). Gradle/Kotlin/AGP upgrade warnings sirf deprecation notices hain — koi action nahi.

## Task: order_taker_app v2 — bold day/time, no timer, paid-to-end, press-fade + dashboard table/takeaway revenue — DONE (Sep 2026)
- **Icon re-confirmed:** Launcher icons dobara source se regenerate kiye (`C:\Users\usman\Downloads\...07_34_24 PM.png`, HighQualityBicubic) — `ic_launcher`/`ic_launcher_round` (48-192) + adaptive foregrounds (108-432); stale `mipmap-xxxhdpi/ic_launcher_foreground.png` delete (bas unused tha, apk icon naya logo hi use karta hai). APK shell script `C:\Users\usman\AppData\Local\Temp\opencode\regen_icons.ps1`.
- **Order card (`order_taker_screen.dart` `_orderCard`):**
  - **⏱ elapsed timer REMOVED** — `valueListenable: _now` wala container gaya, `durationBaseMs()` hata diya, `_now` ValueNotifier + `_tickTimer` (1s) pooray remove (ab koi reader nahi tha). Bottom row ab sirf "Tap for details"/"Hide details" + rotate ▼.
  - **Day + bold time:** subtitle ab `RichText` — **time BOLD in glow color** + `'  ·  $dayStr, $dateStr'` (e.g. `3:45 PM · Mon, 14/09/2026`) + optional `· takerName`. Naye helpers `dayName(iso,{utcOffset})` (Mon..Sun) + `shortDate(iso,{utcOffset})` (dd/MM/yyyy) added next to `time12`.
  - **Paid → list end:** `visibleOrders` + `filteredVisibleOrders` sort ab paid-first-zero compare (paidRank 1 → end), phir createdAt desc. Har tab mein paid orders neeche/end par.
  - **Press-fade whole block:** naya `_PressBlock` StatefulWidget (Listener based, 65ms fade-in, AnimatedScale 0.96 + AnimatedOpacity 0.5, 180ms min hold) — POORA order block wrap karta hai, to kisi bhi action button (ya card tap / select-mode check) par fast fade+scale hota hai. Selection-mode `_bulkBar` ke 4 buttons bhi `_PressBlock` mein wrapped (Delete/Mark Paid/Mark Due/Select All same fade).
  - **Action icons prominent + buttons small:** `_actionChip` ab `(label, IconData, fg, bg, onTap)` — Material vector icon size 14 (Icons.print, currency_rupee_outlined, check_circle_outline, cancel_outlined, delete_outline, done_all) + chhota label 10pt, compact padding (9,6), minWidth constraint hata diya. Bade buttons ke emoji (📷/✅/👤/🖨️/➕/🗑️) bhi Material icons (size 15) kar diye.
- **Dashboard (`dashboard_screen.dart`):**
  - **Table vs Takeaway revenue** alag alag glowing cards — `_loadData` ab `tableRevenue` (Dine-In today) + `takeawayRevenue` (Takeaway today) compute karta hai; `_buildStatsGrid` = Row1 (Today Orders, Total Orders) + Row2 (**Table Revenue** green `0xFF059669` glow + **Takeaway Revenue** amber `0xFFB45309` glow, BoxShadow 0.4/blur18, bold 17pt). `_statCard` ne `{bool glow=true}` param liya.
  - **Refresh working:** header mein refresh IconButton + octal "🔄 Refresh Data" card dono `_loadData(spin: true)` — `_refreshing` spinner (header + card) dikhta hai jab tak saari orders refetch ho; `_loadData` finally-catch mein `_refreshing=false` reset + silent wala path bhi. Saare orders + stats + recent orders refresh.
  - **Order row click → orders screen:** dashboard Recent Orders ke har `ListTile` par `onTap: _openOrderTaker` (nayi screen, action buttons + icons sab saath).
- **Verify:** `flutter analyze --no-pub` NO issues (0). `flutter build apk --release` ok (54.4MB) → `build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\order_taker_app.apk`. Gradle/Kotlin/AGP upgrades sirf warnings.

## Task: Piper — RESUME MARKER (jaise hi user "Piper" type kare, is task se shuru karo)
- **Context:** Aap is session ko "Piper" naam se yaad rakho. User jab "Piper" type karta hai to is section se hi kaam shuru karna (teeno apps: delivery_orders_app / nashta_app / order_taker_app, stock_app, server/routes.js, usman_hotel_app sab D:\pos ke andar, Railway deploy via git push origin main).
- **Latest state (Sep 2026) — stock app message/voice:**
  - **Delete:** App + server double-gated — **sirf manager** role delete kar sakta hai. App: `stock_list_screen.dart` mein delete icon `if (orderId != null && Session.isManager)` (bubble + replies). Server: dono DELETE routes (`/stock/orders/:id/message` aur `/stock/orders/:id/reply/:replyIndex`) par `if (req.user.role !== 'manager') return 403`.
  - **Cashier:** Full bubble render — voice bars, text, photo, timestamps, role chip (yellow `0xFFFACC15`), right-aligned — **admin/manager jaisa hi**, sirf delete icon nahi dikhta. Koi content-hide-gate nahi.
  - **Colors:** admin purple `0xFFA78BFA`, manager teal-green `0xFF0D9488`, cashier clear yellow `0xFFFACC15`. Saare bubbles right-aligned.
  - **Today/Yesterday filter:** stock orders tab mein quick chips (`_datePreset`/`_quickChip`).
  - **Per-item photos:** order card mein har item ka 44px thumbnail + fullscreen overlay (`_photoThumb`/`_viewPhoto`); server `POST /stock/orders` per-item `photo` list store karta hai.
  - **APK/Railway:** `D:\pos\UsmanHotel-StockApp.apk` (52.4MB) naya; server `48dd9b4` pushed → Railway auto-deploy. Verify: `node --check routes.js` NAHI — Flutter `flutter analyze` + `flutter build apk --release` + copy app-release.apk.
- **Agar user ne iske baad kuch naya pucha to:** stock delete sirf manager (cashier ko full view + delete icon nahi), colors, alignment, today/yesterday filter, per-item photos — bas isi ke aas-paas focus; baki apps (delivery/nashta) ko sirf tab touch karo jab us se explicit reference ho.
- **Kya BAKI HAI (pending decisions, user abhi tk confirm nahi):** (1) cashier ko order-card par approve/reject/delete buttons dikhne chahiye ya nahi — ye pucha tha, answer pending. (2) Age* kis cheez par color — aur koi request batao.


## Task: Three apps in ONE (Delivery + Nashta + Order Taker) — REVERTED
- **Goal:** Nashta app aur Order Taker app ko Delivery app mein embed karna tha (aik hi app mein). Nahta app icon + Order Taker icon pick-up screen ke top par.
- **Approach tried:** `path:` dependencies (nashta_app, order_taker_app) delivery pubspec mein + `lib/embedded_apps.dart` (setupEmbeddedApps/openNashtaApp/openOrderTakerApp) + pick-up screen par `_embeddedAppsStrip`.
- **Status:** User ne APPROACH NAPASAND KAR DI — poora update REVERT kar diya gaya. `embedded_apps.dart` delete, pubspec path deps hata, `main.dart`/`order_taker_screen.dart` wapas restore, `build/`, `.dart_tool/`, `android/.gradle` remove kar diye.
- **Lesson:** Ye "embedded apps via nested MaterialApp route" ka rasta kaam nahi. Agar dobara ye task aaye to pehle user se approach confirm karna (e.g. vendored code vs intents vs package deps) kyunki user ko nested-app approach pasand nahi.
- **Environment:** `delivery_orders_app/` (delivery), `nashta_app/` (nashta), `order_taker_app/` (order taker) — teeno alag Flutter apps. Web dashboard `usman_hotel_dashboard/` bhi hai aur `stock_app/` bhi.

## Task: Railway order-save timeout fix — DONE (server `db.js`)
- **Problem:** `order_taker_app` tookaway Pay Now/Pay Later mein "Railway server sync timeout..." (25s POST timeout) dikhata tha, internet theek hone ke bawajood. Root cause: har order create + har item ka stock `updateRecord` ALAG-ALAG call par poora DB Postgres ko upsert hota tha (2.6MB × N) aur `POST /pos/orders` route (`routes.js` ~line 1199) `await waitForPersist()` par block karta tha.
- **Fix (`D:\pos\server\db.js`):** `writeDb()` ab pg mode mein 250ms-debounced coalesced flush karta hai — ek order ke saare writes collapse ho kar EK file write + EK Postgres upsert ban jate hain; `waitForPersist()` ab pending debounce/flush par wait karta hai (~0.5s max). `SIGTERM`/`SIGINT` handler (`flushBeforeExit`) bhi add kiya taake burst flush hone se pehle data na khoye. Local mode (no Postgres) waisa hi sync file write.
- **NO separate web-app/relay exists:** Flutter APK seedha Railway server (`usman-hotel-pos-server-production.up.railway.app`) se baat karta hai jo direct Postgres par save karta hai. "Web app remove" ka matlab sirf server ko fast karna tha — yaad rakhna, aur apps (dashboard web app etc.) alag hain.
- **Verified:** `node --check` all ok; isolated smoke test (local mode) — 6 writes → single flush 333ms, cleanup clean. Deploy ke liye git push → Railway auto-deploy. APK side koi change nahi chahiye (25s timeout ab kabhi nahi lagta).
- **Main app (yad rakho, phir se read mat karna):** `D:\pos\usman_hotel_app` ek single Flutter app hai jo launcher (`lib/launcher_screen.dart`, `lib/main.dart`) ke through embedded delivery + nashta order-taker screens kholti hai. Embedded delivery ka code `lib/embedded/delivery/` mein hai — `dashboard_screen.dart` + `order_taker_screen.dart` farigh updater isi app ke andar edits karte hain (alag `delivery_orders_app/` NOT the target). Entry files: `lib/main.dart` (`lib/embedded/delivery/main.dart`), `launcher_screen.dart`. Build: `flutter build apk` inside `D:\pos\usman_hotel_app` → APK `build\app\outputs\flutter-apk\app-release.apk`; extra copy `D:\pos\usman_hotel_app.apk` bhi banai jaati hai.
- **Completed updates (Sep 2026, usman_hotel_app embedded delivery):**
  - Dashboard (`dashboard_screen.dart`): `_prodCats` map (`GET /pos/products` se, keyed `id:<id>`/`name:<name>` lowercase); revenue ab `_revenueOf(o) = total/amount + deliveryFee + extras amount` (extras = items jinki product category mein 'extra' ya 'addon' ho). `_markDuePaid` merge-group atomic hai (poora `mergeGroupId` group paid+payment, `Future.wait`). `_miniBtn` + merge-bar buttons `_PressFx` (press fade) par hain.
  - Orders screen (`order_taker_screen.dart`): `markPaid` group-atomic (≥2 merge members → `_bulkMarkPaidList`). `_orderCard` redesign — rider name TOP bold glowing blue `0xFF1D4ED8` + `_deliveryPill()` (blue DELIVERY tag wala, 'Walk-in' fallback HATA diya), amount + RESERVED chip right, Order # sab se last bottom row. `_mergedMemberRow` + `_mergedOrdersCard` bhi redesign (rider glow, delivery pill, order # last). `_actBtn`/`_hdrBtn`/cart create-btn/quick-pickup Mark Due sab `_PressFx` press-fade.
  - Helpers (order_taker_screen mein): `_glowText(String s, {color, size, weight, maxLines})`, `_deliveryPill()`, `_PressFx`/`_PressFxState` (Listener based, AnimatedScale 0.92 + AnimatedOpacity 0.65, 80ms) — iska copy `dashboard_screen.dart` ke end par bhi hai.
- **Server:** `D:\pos\server\routes.js` — `/pos/products` (~line 945), `orderRevenue = baseTotal - extras - deliveryFee` (extras/delivery analytics se exclude) (~line 656-684).
- **BUG FIXED (important):** `_PressFx` sirf press animation karta tha, `widget.onTap` call NAHI karta tha — is liye `_hdrBtn` (Orders/Dash/Printer/Exit) aur dashboard `_miniBtn` dead thay. Ab `onPointerUp` par `widget.onTap?.call()` bhi hota hai. Jahan inner widget ka apna handler ho (`_actBtn`, cart create, quick-pickup Mark Due, dashboard merge-bar) wahan `_PressFx` ko `onTap` MAT do warna double-fire hoga.
- **PERFORMANCE FIXES (Sep 2026, `D:\pos\order_taker_app` — manager-role order taker app, alag standalone app):** Yaad rakho: `order_taker_app/` (order taker), `delivery_orders_app/` (delivery), `nashta_app/` (nashta) TEENO ALAG apps hain. User ne kaha: takeaway Pay Later/Pay Now buttons `order_taker_app` mein hain; delivery/nashta embedded (usman_hotel_app) ko IS task mein touch nahi karna. Changes sirf `D:\pos\order_taker_app\lib\order_taker_screen.dart` mein kiye:
  - `_loadData` ab 2-phase hai: pehli load par sirf critical (categories, products, tables, orders) ka `Future.wait` → `initialLoading=false` turant, phir background mein `_loadSecondaryData()` (settings + mashallah-slots). Silent (20s timer) wala path waisa hi full-6 + `_sameAsOnScreen` signature raha (blink na ho).
  - Har order-action ke baad `await _loadData(silent: true)` (6 endpoints, heavy products/base64 samet) → `await _refreshOrdersOnly()` (sirf `/pos/orders`, 1 endpoint). Table occupancy `orders` list se derive hoti hai (server `tables` se nahi), is liye orders-only refresh kafi hai.
  - `createOrder` mein payment POST ab fire-and-forget hai (`.catchError((_) => null)`), order save pe button turant release hota hai; print bhi save ke turant baad start hota hai.
  - Analyze clean, APK: `D:\pos\order_taker_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\order_taker_app.apk`.
- **Verify:** `flutter analyze --no-pub` clean (sirf pre-existing infos), `flutter build apk` ok.

## Task: Order Taker offline-first (save + print + view without internet, auto-sync) — DONE (Sep 2026)
- **Requirement:** User ne kaha — "print lazmi, order save lazmi pehly memory mein, phir internet aaye to Railay/Postgres mein save ho jaye, orders bina internet bhi view hon." Aur pehla question: "cannot load tables from server" toasts local Postgres ki wajah se hain? **Answer: NAHI** — app seedha Railway se baat karta hai (`api.dart` defaultHost = Railway URL), local Postgres (localhost:5432) irrelevant hai; toast sirf connectivity/token issue tha. Local Node server (port 4000) is NOT running; `server/.env` gitignored hai.
- **Server (`D:\pos\server\routes.js`):** `POST /pos/orders` ab idempotent hai — `clientId` destructure + agar already order (same clientId) mojood to 201 return (duplicate na bane). Aur client ka `orderNumber` / `createdAt` / `updatedAt` honor hota hai (`orderNumber: req.body.orderNumber || \`ORD-${getNextOrderNumber()}\``, `createdAt`/`updatedAt` = `req.body.createdAt` agar diya ho). Order record mein `clientId` bhi store hota hai. `node --check` OK.
- **App (Naya file `D:\pos\order_taker_app\lib\offline_store.dart`):** SharedPreferences-based — `loadPendingOrders/savePendingOrders/addPendingOrder(dedupe by clientId, insert front)/removePendingOrder`, `loadMutations/saveMutations/addMutation/removeMutation/compactMutations` (last-write-wins per order id), `loadCache/saveCache` (`ot_server_cache_v1`), `nextClientId()` → `ot-<millis>-<seq>` (prefs `ot_seq`). isme `_seq` field wala code remove kar diya (unused).
- **App (`order_taker_screen.dart`) offline wiring:**
  - Naye fields: `pendingLocalOrders` (List<Map>), `serverOrders` (server snapshot for `_sameAsOnScreen` — isi se silent-refresh sig compare hota hai, merged orders se nahi), `offline`, `syncing`. Bug bhi note karo: pyhle `_sameAsOnScreen` q orders ko compare karta tha. Ab `serverOrders` se.
  - `_loadData`/`_refreshOrdersOnly`: success par `serverOrders` set + `_mergePendingOrders()` + `_saveCacheSnapshot()`; failure/fetch-null par `offline=true` aur agar kuch data na ho to `_loadOfflineSnapshot()` (cached categories/products/tables/orders/settings/mashallahSlots se UI restore). "Could not load tables" toast ab SIRF tab jab orders bhi empty hon (yaani sach mein koi cached data nahi).
  - `_mergePendingOrders()`: `orders = serverOrders + pendingLocalOrders` (pending jo abhi tak server par confirm nahi). Silent timer loop ab offline+all-null par wapas ja kar rebuild nahi karta (blink fix).
  - `_syncPendingOrders()` (`syncing` guard): pehle queued mutations replay (DELETE/PUT `/pos/orders/:id`), phir pending orders POST (idempotent clientId se), agar pending order `paid` hai to sync ke baad `/pos/payments` fire-and-forget (payMethod/cashReceived stored entry se), phir `_refreshOrdersOnly()`. Recurrence se `syncing` flag bachata hai (refresh ke andar sync wapas call hota hai to early-return).
  - `createOrder`: payload mein `clientId` add; `ApiException statusCode == 0` (network/timeout) par `_stageOrderLocally()` — display order bana (temp `orderNumber: OT-<clientId>`, locally computed total), `OfflineStore.addPendingOrder`, `pendingLocalOrders` + UI merge, cart clear, print turant (returned map print flow mein chala jata hai — offline payment ke liye `created['localPending'] != true` guard rakha taake `/pos/payments` 404 na kare), toast "saved locally, will sync". Asli server errors (statusCode 4xx/5xx) ab bhi dikhte hain (offline queue mein NAHI jate).
  - `_offlineAwareOrderCall(order, method, body)` helper: online → direct fetch; network-error (statusCode 0) → `addMutation` queue + optimistic local patch (UI) + `unawaited(_syncPendingOrders())`; local pending orders (`localPending: true`) → `_applyPendingLocalPatch` (display + stored entry['payload'] dono patch, taake sync par final state jaye). Real server error → `rethrow` (toast) — offline sirf network errors queue hoti hain.
  - Singled mutations (`markServed/markDue/markPaid/cancelOrder/deleteOrder/saveEditOrder/attachPaymentPhoto/pushOwnerRequest`) + bulk (`_bulkDelete/_bulkMarkPaid/_bulkMarkDue`) sab `_offlineAwareOrderCall` par routed — offline toasts "(offline - auto-sync)".
  - `initState` mein `_restorePendingOrders()` — startup par stored pending orders wapas `pendingLocalOrders` mein load + merge + sync try.
  - Offline UI: main screen par red banner "No internet — N pending order(s) saved locally, auto-syncing when connected" (`offline == true` par, `_searchBar` ke neeche); order card par `localPending == true` orders ke paas amber "PENDING" chip. Reconnect par silent `_loadData` success → `offline=false` + `_syncPendingOrders()` (silent branch mein bhi).
- **Verify:** `flutter analyze --no-pub` clean (0 issues). `flutter build apk --release` ok → `D:\pos\order_taker_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\order_taker_app.apk`.
- **Deploy note:** Server fix Railway par jaane ke liye `git push` (Railway auto-deploy) — APK side hi changes hain, dono deploy chahiye.

## Task: Embedded Delivery + Nashta offline-first + green/red sync indicator — DONE (Sep 2026, usman_hotel_app)
- **Scope:** Usman Hotel App (`D:\pos\usman_hotel_app`) ke embedded delivery (`lib/embedded/delivery/order_taker_screen.dart`) aur embedded nashta (`lib/embedded/nashta/order_taker_screen.dart`) dono order-taker screens par vahee offline-first port jaise `order_taker_app` mein tha. Standalone `delivery_orders_app/`/`nashta_app/` touch NAHI kiye.
- **Shared store (`lib/embedded/offline_store.dart`):** namespaced version (order_taker_app wale ka copy) — har screen apna `namespace` set karta hai: delivery = `'dlv'`, nashta = `'nsh'` (default `'otu'`). Keys: `<ns>_pending_orders_v1`, `<ns>_pending_mutations_v1`, `<ns>_seq`, `<ns>_sync_ok_v1`. **Cache key bhi namespaced** (`<ns>_ot_server_cache_v1`) — pehle shared tha, delivery/nashta ek doosre ki cache clobber kar sakte thay; fix kar diya.
- **Green/red sync indicator (user ka NEW requirement):** `bool? lastSyncOk` field (null = unknown) + `OfflineStore.loadSyncOk()/saveSyncOk()` persistence (`<ns>_sync_ok_v1`). `_syncPendingOrders()` success par `_markSyncOk()` (green), koi item fail ho to `_markSyncFailed()` (red). Successful `_loadData`/`_refreshOrdersOnly` bhi green deta hai (jab koi pending order/mutation na ho). Offline failure paths, `_offlineAwareOrderCall` queue, aur `_stageOrderLocally` → red + `saveSyncOk(false)` set karte hain. UI: delivery main screen par red quick-due "+" button ke aage; nashta orders-screen header (Close icon ke paas) — 34px circle: green `cloud_done` / red `cloud_off` / gray `cloud_outlined` / blue spinner (syncing); tap → manual `_syncPendingOrders()`.
- **Delivery screen ported:** import + fields (`pendingLocalOrders/serverOrders/offline/syncing/lastSyncOk`) + initState (`namespace='dlv'`, `_restorePendingOrders`, `loadSyncOk`); `_sameAsOnScreen` ab `serverOrders` se compare; new `_loadData` (11 endpoints, offline cache+merge+sync trigger+green), new `_refreshOrdersOnly`, helpers `_mergePendingOrders/_saveCacheSnapshot/_loadOfflineSnapshot/_restorePendingOrders/_offlineAwareOrderCall/_applyPendingLocalPatch/_stageOrderLocally(deliveryFee samet)/_syncPendingOrders/_markSyncOk/_markSyncFailed`; `createOrder` → `clientId` + `_stageOrderLocally` fallback (payment/customer POST sirf `localPending != true` par, toast offline wala, print turant); `_putOrder/_deleteOrder/_bulkDeleteList/_bulkRiderList/_bulkMarkPaidList/_bulkMarkDueList/_bulkMarkReservedList` + edit-modal save sab `_offlineAwareOrderCall` par; `markPaid` payment POST offline par skip. UI: `_offlineBanner()` (red, `_dbWarningStrip` ke neeche), order card par amber "PENDING SYNC" chip. **Analyzer gotcha:** `createOrder` mein `created0` nullable ko `is Map` promote NOI hota (capture/closure ki wajah se bhi) — ab `final created = created0 ?? {...}; final createdOk = created0 != null;` pattern use hota hai, `created['...']` non-null par clean.
- **Nashta screen ported:** same wiring, sirf `namespace='nsh'`, `_loadData` apna existing 2-phase (critical categories/products pehle → phir full 11) preserve kiya + offline logic add; `_syncIndicator` nashta orders-screen header (Nashta title bar, Close ke paas); `_orderCard` ListTile subtitle Row mein "PENDING SYNC" chip.
- **Verify:** `flutter analyze --no-pub` clean (sirf pre-existing `_setBt` unused warnings + infos), `flutter build apk --release` ok (54.4MB) → `D:\pos\usman_hotel_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\usman_hotel_app.apk`. Server (idempotent POST clientId) pehly he pushed tha — koi naya server change NAHI.

## Task: Stock App — photo attach (camera), admin login (orders-only), admin per-order messages — DONE (Sep 2026, stock_app + server)
- **Scope:** `D:\pos\stock_app` (standalone stock app) + `D:\pos\server\routes.js` stock endpoints.
- **Server (`routes.js`):** `/stock/login` ab `admin` role bhi allow karta hai — role logic separate: `isAdmin` (`/admin/i`) → role `'admin'`, `isManager` (`/manager/i`) → `'manager'`, `isCashier` → `'cashier'`; error msg "Only Admin, Manager and Cashier...". `POST /stock/orders` ab extra `photo` field accept/store karta hai (`photo: photo || ''`). Naya route `PUT /stock/orders/:id/message` — **admin-only** (`req.user.role !== 'admin'` → 403) — body `{type, text}`, order par `message: {type, text, sentBy, sentAt}` set karta hai (approve/reject jaisa db mutation pattern). `node --check` OK.
- **App (stock_app):** pubspec mein `image_picker: ^1.1.2` add; `AndroidManifest.xml` mein `<uses-permission android:name="android.permission.CAMERA"/>`. `api.dart` → `sendStockOrderMessage(id, type, text)` helper (PUT). `session.dart` → `isAdmin` getter.
- **Add Stock tab (`add_stock_screen.dart`):** naya "Photo Attach (Camera)" section card (items ke baad, Order Details se pehle) — `ImagePicker().pickImage(source: camera, maxWidth/maxHeight 1200, quality 80)` → `data:image/jpeg;base64,...` (attachPaymentPhoto wala pattern, imageQuality 80), thumbnail preview (110x110) + Retake/Remove buttons + tap preview → full photo dialog (`InteractiveViewer`). `_save()` payload mein `'photo': _photoDataUrl`; success par clear. Helpers: `_photoBytes` getter (base64Decode after first comma), `_attachPhoto`, `_viewPhoto`, `_buildPhotoAttach`, `_smallPhotoAction`.
- **Orders tab (`stock_list_screen.dart`):** order card ab photo dikhata hai (`_photoBytes(order['photo'])`) — 170px cover image + gradient + "Tap to view full photo" overlay, tap → `_viewPhoto` full-screen dialog (InteractiveViewer maxScale 5, close button). `order['message']` (Map) ho to `_messageSection` render hota hai — type chip (color per type: Note=purple/Query=blue/Instruction=green/Warning=amber/Important=red), sender + text + sentAt — **manager aur cashier dono ko** order ke saath dikhta hai. Admin ke liye action row mein Print/Approve/Reject/Delete ki jagah sirf purple "Send Message" button (`_adminSendMessage`) — dialog: order #, Message Type dropdown (`_messageTypes` const list), multiline text (max 300) → `sendStockOrderMessage` → refresh. `_msgTypeColor`, `_photoSection`, `_adminMessageButton` helpers. Non-admin actions unchanged (Print sab ko, Approve/Reject/Delete sirf manager).
- **Admin role gating:** `dashboard_screen.dart` — `Session.isAdmin` par sirf StockListScreen tab (`Orders`), Add Stock/Headings/Settings hidden, bottomNavigationBar `null` (single tab), role chip purple `0xFFA78BFA`. `login_screen.dart` footer → "Admin, Manager & Cashier". Admin server-par bhi view-only hai (manager-only routes 403 dete hain kyunki role `'admin'` hai) — sirf message send kar sakta hai.
- **Verify:** `flutter analyze --no-pub` — koi error/warning nahi (sirf pre-existing `withOpacity`/`activeColor` deprecation infos). `flutter build apk --release` ok (51.4MB) → `D:\pos\stock_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\UsmanHotel-StockApp.apk` (convention: **capital** `UsmanHotel-StockApp.apk`).
- **Deploy note:** Server changes Railway par jaane ke liye `git push` (auto-deploy) + naya APK. Admin login ke liye `staff` collection mein role `admin` wala user banana hoga (server DB, e.g. `staff` record). Photo base64 DB mein ubarti hai (pos_products photos jaisa pattern) — Railway par DB.json bhi patla hota jayega agar bari photos ho.

## Task: New app icon replace (usman_hotel_app) — DONE (Sep 2026) + Merged-orders sheet fixes
- **Icon:** User ne naya logo dediya (`ChatGPT Image Sep 13, 2026, 03_23_42 AM.png`, 1254×1254, Format24bppRgb — no alpha). Source copy: `D:\pos\usman_hotel_app\assets\icon\app_icon.png`. Saare launcher icons generate kiye (HighQualityBicubic downscale, koi upscale nahi = sharp): `mipmap-{mdpi48,hdpi72,xhdpi96,xxhdpi144,xxxhdpi192}/ic_launcher.png` + adaptive foreground `drawable-{mdpi108,hdpi162,xhdpi216,xxhdpi324,xxxhdpi432}/ic_launcher_foreground.png` (full-bleed image; `ic_launcher.xml` ka 16% inset + background `#38BDF8` ring rakh liya). Power nail: System.Drawing se PNG encoder (ColorDepth 32) + InterpolationMode.HighQualityBicubic.
- **Same build mein merged-sheet fixes bhi compile huan:** `_DashboardScreenState` mein `_fadeWrap(key, child)` (AnimatedOpacity→0 + AnimatedScale→0.9, 380ms); `_mergedCard(String groupId, {bool pending, String tab})` — tab-aware colors (paidTab green 0xFF047857/0xFFF0FDF4), fade wrap `g:$groupId`, action row per tab (paid: Print/Delete; reserved: Print/Mark Paid/Mark Due/Delete; due: Print/Mark Paid/**Return to Active**/Delete); `_markGroupReturnToActive(groupId)` group-atomic (status Active + paymentStatus Pending, fade). `_unmergeSingle` ab `Future<bool>` (pop hataya, local merge fields clear), `_markOrderDueDash` `Future<bool>`. **Naya widget `_MergedSheet`** (StatefulWidget, `parent` + `members`) replaces inline `_openMergedSheet`/`_mergedOrderDetailCard`: local `_fading`/`_busy`, `_run(fn, o, {remove})` — success+remove → fade (350ms) + member remove, empty → auto-close. Cards: Remove from Merge / Mark Paid / Mark Due / Delete. Gotchas: `_mergeBox` `_DashboardScreenState` ka static const hai (library-level nahi) → `_DashboardScreenState._mergeBox`; parent state methods ko tear-off na do, closure `() => p._method(o)` do (argument_type_not_assignable).
- **Verify:** `flutter analyze --no-pub` 0 errors (sirf pre-existing infos/warnings), `flutter build apk --release` ok (54.8MB) → `D:\pos\usman_hotel_app\build\app\outputs\flutter-apk\app-release.apk` + copy `D:\pos\usman_hotel_app.apk`.

## Note: Staff/cashier delete se orders DELETE nahi hote (FAQ — yaad rakhna)
- **Question:** Agar cashier/staff ka account delete kar dein to us ke banaye orders bhi delete ho jate hain? **Answer: NAHI.**
- **Procedure:** Staff delete sirf web dashboard (`usman_hotel_dashboard` → Staff screen → Delete) se hota hai → `DELETE /staff/:id` (`D:\pos\server\routes.js:878` generic collection delete) → `removeRecord('staff', id)` — sirf staff record remove hota hai, koi cascade nahi.
- **Orders ka relation:** Orders mein staff ka naam plain string `orderTaker`/`waiter` field mein store hai (`_putOrder`/`createOrder` mein `'orderTaker': me`, order_taker_screen.dart ~557). Staff id FK nahi hai — is liye staff delete se orders/profit/revenue/analytics sab pehle jaise rehte hain (name waisa hi dikhta hai).
- **Cascade sirf pos_orders par:** `DELETE /pos/orders/:id` cleanup karta hai (rider_order_requests + rider_orders). Staff par koi cascade/cleanup nahi.
## Task: Stock app strict roles + photo verify (local proof) � DONE (Sep 2026, server only)
- **User wants:** sirf exact Administrator? NO � sirf exact role Admin ? ADMIN, Manager ? MANAGER, Cashier ? CASHIER. Substring matching (pichle d4ecee6) ne Admin Order Taker/Admin Rider ko bhi admin bana diya tha � user ne mana kiya.
- **Fix (server/routes.js /stock/login ~line 425):** STRICT mapping ^^admin^^i/^^manager^^i/^^cashier^^i (exact match); baqi sab roles (Admin Order Taker, Admin Rider, Waiter, Biker, Takeaway Order Taker, Table Order Taker) ? 403 Only Admin, Manager and Cashier roles can login to the Stock App. App side (Session.isAdmin/isManager) pehle se exact tha.
- **Photo issue root cause:** app code DURUST hai (camera ? base64 data URL ? POST photo ? GET par photo render). Problem ye tha ke DEPLOYED Railway server OLD commit par tha jo photo field store hi nahi karta � is liye photo order par show nahi hui + admin accounts MANAGER dikhte thay. Fix = push latest server/routes.js (usme photo store + approve/reject admin + strict roles).
- **Verified LOCALLY (photo-roundtrip-test.mjs pattern, disposable):** local mode (DATABASE_URL='' + PORT=4001) par 5 temp staff with known bcrypt hash: exact Admin?admin(200), Manager?manager(200), Cashier?cashier(200); Admin Order Taker?403, Waiter?403. POST /stock/orders photo===sent true; GET roundtrip true; admin DELETE 403; manager DELETE 200. db.json backup/restore karke test clean.
- **User impact:** Usman ke Admin Order Taker accounts ab stock app login NAHI kar sakte jab tak role exact Admin na change ho (web dashboard Staff screen se). Manager/Cashier waisay hi chalte hain (ab sahi tags ke saath).
- **Deploy:** git push ? Railway auto-deploy. NEW APK NAHI bani (sirf server change).
